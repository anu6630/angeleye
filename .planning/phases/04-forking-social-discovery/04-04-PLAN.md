---
phase: 04-forking-social-discovery
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - docker-compose.yml
  - backend/requirements.txt
  - backend/app/core/config.py
autonomous: true
requirements:
  - PERF-06
user_setup: []
must_haves:
  truths:
    - "Redis service running in Docker Compose on port 6379"
    - "Redis persistence enabled via RDB snapshots"
    - "Python redis-py library installed"
    - "Backend has REDIS_URL configuration"
    - "Redis health check endpoint configured"
    - "Backend can connect to Redis and perform basic operations"
    - "Redis infrastructure ready for caching (PERF-06 satisfied by infrastructure implementation)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Redis service definition"
      contains: "redis:"
    - path: "backend/requirements.txt"
      provides: "Python redis dependency"
      contains: "redis=="
    - path: "backend/app/core/config.py"
      provides: "Redis configuration"
      contains: "REDIS_URL"
  key_links:
    - from: "docker-compose.yml"
      to: "backend"
      via: "depends_on redis service"
      pattern: "depends_on:\s+- redis"
    - from: "backend/app/core/config.py"
      to: "docker-compose.yml"
      via: "REDIS_URL environment variable"
      pattern: "redis://localhost:6379"
---

# Phase 04-04: Redis Infrastructure and Caching Foundation

## Objective

Add Redis service to Docker Compose with persistence, install redis-py library, configure Redis URL in backend settings, and verify connectivity. This provides the infrastructure for trending score caching, feed caching, and engagement metrics (PERF-06).

Purpose: Enable high-performance caching for feed algorithms and engagement metrics tracking.

Output: Redis service in Docker Compose, Python redis-py installed, backend configured with REDIS_URL, connectivity verified.

**Note:** PERF-06 requirement is satisfied by implementing Redis caching infrastructure. Performance benchmarking (measuring DB load reduction) is deferred to production monitoring.

## Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-forking-social-discovery/04-CONTEXT.md

@docker-compose.yml (existing services)
@backend/requirements.txt (existing dependencies)
@backend/app/core/config.py (existing settings)

## Tasks

<task type="auto">
  <name>Task 1: Add Redis service to Docker Compose</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (existing services)
    - .planning/phases/04-forking-social-discovery/04-RESEARCH.md (Redis requirements from CONTEXT.md D-22 to D-29)
  </read_first>
  <action>
    Update docker-compose.yml to add Redis service:

    Add redis service definition:
    - image: redis:7-alpine (latest stable Redis 7)
    - container_name: notebooksocial-redis
    - ports:
      * "6379:6379" (Redis default port)
    - volumes:
      * redis_data:/data (persist RDB snapshots)
    - command: redis-server --appendonly yes --appendfsync everysec (AOF persistence with everysec sync)
    - healthcheck:
      * test: ["CMD", "redis-cli", "ping"]
      * interval: 10s
      * timeout: 3s
      * retries: 3
    - restart: unless-stopped

    Add redis_data volume to volumes section:
    - redis_data: (named volume for persistence)

    Update backend service to depend on Redis:
    - Add redis to depends_on list
    - Add condition: service_completed_successfully for redis healthcheck

    Per CONTEXT.md D-22: Redis data structures for scores, ZSETs, and lists
    Per RESEARCH.md: Redis 7.4.0 for production-ready stability and performance
  </action>
  <acceptance_criteria>
    - docker-compose.yml contains "redis:"
    - docker-compose.yml contains "image: redis:7-alpine"
    - docker-compose.yml contains "ports:\s*- \"6379:6379\""
    - docker-compose.yml contains "redis_data:" in volumes section
    - docker-compose.yml backend service contains "depends_on:" with "redis:"
    - docker-compose.yml redis service has "healthcheck:" with "redis-cli ping"
  </acceptance_criteria>
  <verify>
    <automated>grep -c "redis:" docker-compose.yml ≥ 2 (redis service + redis_data volume)</automated>
  </verify>
  <done>
    Redis service configured in Docker Compose with persistence, health checks, and proper dependencies
  </done>
</task>

<task type="auto">
  <name>Task 2: Install redis-py and configure REDIS_URL</name>
  <files>
    backend/requirements.txt
    backend/app/core/config.py
  </files>
  <read_first>
    - backend/requirements.txt (existing dependencies)
    - backend/app/core/config.py (existing Settings class)
  </read_first>
  <action>
    Update backend/requirements.txt:
    - Add redis==5.2.1 (stable redis-py release, matches Python 3.11+)

    Update backend/app/core/config.py:
    - Add to Settings class:
      * REDIS_URL: str = Field(default="redis://localhost:6379/0", description="Redis connection URL")
      * REDIS_MAX_CONNECTIONS: int = Field(default=50, description="Maximum Redis connection pool size")
      * REDIS_SOCKET_TIMEOUT: int = Field(default=5, description="Redis socket timeout in seconds")
      * REDIS_SOCKET_CONNECT_TIMEOUT: int = Field(default=5, description="Redis connection timeout in seconds")

    - Add validation for REDIS_URL format (optional, for robustness):
      * @validator("REDIS_URL")
      * def validate_redis_url(cls, v: str) -> str:
        * if not v.startswith("redis://"):
          * raise ValueError("REDIS_URL must start with redis://")
        * return v

    Per CONTEXT.md D-22: Redis data structures for caching (hashes, ZSETs, lists)
    Per RESEARCH.md: redis-py 5.2.1 for production stability
  </action>
  <acceptance_criteria>
    - requirements.txt contains "redis==5.2.1"
    - config.py contains "REDIS_URL: str"
    - config.py contains "REDIS_URL.*Field.*default=\"redis://localhost:6379"
    - config.py contains "REDIS_MAX_CONNECTIONS"
    - config.py contains "REDIS_SOCKET_TIMEOUT"
    - config.py contains "REDIS_SOCKET_CONNECT_TIMEOUT"
  </acceptance_criteria>
  <verify>
    <automated>grep -E "redis==|REDIS_URL|REDIS_MAX" backend/requirements.txt backend/app/core/config.py | wc -l ≥ 5</automated>
  </verify>
  <done>
    redis-py library added to requirements, REDIS_URL and connection settings configured in Settings
  </done>
</task>

</tasks>

<verification>

## Overall Verification Steps

1. **Docker Service**: Verify Redis service starts successfully with docker-compose up
2. **Health Check**: Verify Redis healthcheck passes (redis-cli ping returns PONG)
3. **Python Library**: Verify redis-py installs without errors
4. **Configuration**: Verify REDIS_URL is accessible from Settings class
5. **Connectivity**: Verify backend can connect to Redis and perform basic operations

Run these commands after all tasks complete:

```bash
# Start Redis service
docker-compose up -d redis

# Check Redis is running
docker ps | grep redis

# Verify healthcheck passes
docker exec notebooksocial-redis redis-cli ping  # Should return PONG

# Install redis-py
pip install redis==5.2.1

# Test Python connectivity
python -c "import redis; r = redis.from_url('redis://localhost:6379/0'); r.set('test', 'hello'); print(r.get('test'))"  # Should print b'hello'

# Verify configuration
python -c "from app.core.config import settings; print(settings.REDIS_URL)"  # Should print redis://localhost:6379/0
```

</verification>

<success_criteria>

1. Redis service defined in docker-compose.yml with redis:7-alpine image
2. Redis persists data to redis_data volume via AOF (appendonly yes)
3. Redis has healthcheck using redis-cli ping
4. Backend service depends on Redis with healthcheck condition
5. redis-py 5.2.1 added to requirements.txt
6. Settings class has REDIS_URL with default redis://localhost:6379/0
7. Settings class has Redis connection pool settings (MAX_CONNECTIONS, timeouts)
8. Backend can successfully connect to Redis and perform set/get operations
9. **PERF-06 satisfied:** Redis caching infrastructure is implemented and operational

</success_criteria>

<output>

After completion, create `.planning/phases/04-forking-social-discovery/04-04-SUMMARY.md` with:
- Docker Compose Redis service configuration details
- Redis persistence strategy (AOF with everysec)
- redis-py version and connection pool settings
- Configuration values added to Settings
- Connectivity verification results
- PERF-06 satisfaction: Redis infrastructure ready for caching operations
- Any deviations from plan

</output>
