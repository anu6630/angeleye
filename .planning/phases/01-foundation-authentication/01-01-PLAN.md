---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/, backend/, docker-compose.yml]
autonomous: true
requirements: [INFRA-01, INFRA-02]
must_haves:
  truths:
    - Docker Compose configuration is syntactically valid
    - Frontend and backend directories follow monorepo structure
    - Docker Compose can start all services successfully
  artifacts:
    - path: docker-compose.yml
      provides: "Orchestration for frontend, backend, PostgreSQL, Redis"
      contains: "services: postgres, redis, backend, frontend"
    - path: backend/
      provides: "FastAPI backend directory structure"
      min_lines: 1
    - path: frontend/
      provides: "Next.js frontend directory structure"
      min_lines: 1
  key_links:
    - from: docker-compose.yml
      to: backend/app/main.py
      via: Dockerfile build context
      pattern: "context: ./backend"
    - from: docker-compose.yml
      to: frontend/
      via: Dockerfile build context
      pattern: "context: ./frontend"
---

<objective>
Create the foundational project structure and Docker infrastructure for local development.

Purpose: Establish the monorepo architecture (separate frontend/backend folders) and Docker Compose orchestration that will support all development work. This is the infrastructure foundation that all other plans depend on.

Output: Docker Compose configuration, backend and frontend directory structures, and base Dockerfile configurations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo directory structure</name>
  <files>frontend/, backend/, .gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
Create the monorepo structure with separate frontend and backend directories per INFRA-01:

1. Create /frontend directory (empty for now, will be populated in Plan 06)
2. Create /backend directory with structure:
   - backend/app/ (main application code)
   - backend/alembic/ (migrations)
   - backend/tests/ (tests)
3. Create/update .gitignore with standard Python and Node.js ignores:
   - Python: __pycache__/, *.pyc, .env, .pytest_cache/, *.egg-info/, dist/
   - Node.js: node_modules/, .next/, .env.local, .env.*.local, npm-debug.log*

Do NOT install any dependencies yet - that happens in later plans.
</action>
  <verify>
    <automated>test -d /Volumes/SSDX/codes/time/backend && test -d /Volumes/SSDX/codes/time/frontend && grep -q "__pycache__" /Volumes/SSDX/codes/time/.gitignore && grep -q "node_modules" /Volumes/SSDX/codes/time/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Directory /Volumes/SSDX/codes/time/backend exists
    - Directory /Volumes/SSDX/codes/time/frontend exists
    - File .gitignore contains "__pycache__"
    - File .gitignore contains "node_modules"
    - File .gitignore contains ".env"
  </acceptance_criteria>
  <done>Monorepo structure created with /backend and /frontend directories, .gitignore configured</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose configuration</name>
  <files>docker-compose.yml</files>
  <read_first>N/A (new file)</read_first>
  <action>
Create docker-compose.yml at project root with the following services (per D-13, D-14, D-15):

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:17-alpine
    container_name: notebooksocial-postgres
    environment:
      POSTGRES_USER: notebooksocial
      POSTGRES_PASSWORD: notebooksocial_password
      POSTGRES_DB: notebooksocial
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U notebooksocial"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: notebooksocial-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: notebooksocial-backend
    environment:
      - DATABASE_URL=postgresql://notebooksocial:notebooksocial_password@postgres:5432/notebooksocial
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - FACEBOOK_CLIENT_ID=${FACEBOOK_CLIENT_ID}
      - FACEBOOK_CLIENT_SECRET=${FACEBOOK_CLIENT_SECRET}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - /app/__pycache__
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: notebooksocial-frontend
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
      - NEXT_PUBLIC_GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - NEXT_PUBLIC_FACEBOOK_CLIENT_ID=${FACEBOOK_CLIENT_ID}
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev

volumes:
  postgres_data:
  redis_data:
```

Key configuration details per D-14 and RESEARCH.md:
- PostgreSQL 17 with healthcheck and proper ports
- Redis 7 with persistence and healthcheck
- Backend depends on healthy postgres/redis (per PITFALL 7)
- Frontend depends on backend
- Environment variables for OAuth and secrets
- Volume mounts for hot-reload development
</action>
  <verify>
    <automated>docker compose config > /dev/null 2>&1 && grep -q "services:" /Volumes/SSDX/codes/time/docker-compose.yml && grep -q "postgres:17-alpine" /Volumes/SSDX/codes/time/docker-compose.yml && grep -q "redis:7-alpine" /Volumes/SSDX/codes/time/docker-compose.yml && grep -q "condition: service_healthy" /Volumes/SSDX/codes/time/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - File docker-compose.yml exists at project root
    - docker-compose.yml contains service "postgres" with image "postgres:17-alpine"
    - docker-compose.yml contains service "redis" with image "redis:7-alpine"
    - docker-compose.yml contains service "backend" with context "./backend"
    - docker-compose.yml contains service "frontend" with context "./frontend"
    - docker-compose.yml contains healthcheck for postgres service
    - docker-compose.yml contains healthcheck for redis service
    - docker-compose.yml contains "condition: service_healthy" for backend dependencies
    - docker compose config validates without errors
  </acceptance_criteria>
  <done>Docker Compose configuration created with postgres, redis, backend, and frontend services</done>
</task>

<task type="auto">
  <name>Task 3: Create backend Dockerfile and requirements</name>
  <files>backend/Dockerfile, backend/requirements.txt, backend/.env.example</files>
  <read_first>N/A (new files)</read_first>
  <action>
Create backend Dockerfile, requirements.txt, and .env.example:

1. backend/Dockerfile:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements and install Python dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY . .

# Create non-root user
RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser

# Expose port
EXPOSE 8000

# Run application
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

2. backend/requirements.txt with packages from RESEARCH.md standard stack:
```
fastapi==0.135.3
uvicorn[standard]==0.34.0
sqlalchemy==2.0.48
alembic==1.18.4
psycopg2-binary==2.9.11
python-jose[cryptography]==3.5.0
authlib==1.6.9
httpx==0.28.1
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.7.1
slowapi==0.1.9
redis==5.2.1
pytest==9.0.2
pytest-asyncio==1.3.0
python-dotenv==1.0.1
```

3. backend/.env.example:
```
DATABASE_URL=postgresql://notebooksocial:notebooksocial_password@postgres:5432/notebooksocial
REDIS_URL=redis://redis:6379/0
SECRET_KEY=your-secret-key-here-change-in-production
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
FACEBOOK_CLIENT_ID=your-facebook-client-id
FACEBOOK_CLIENT_SECRET=your-facebook-client-secret
```
</action>
  <verify>
    <automated>test -f /Volumes/SSDX/codes/time/backend/Dockerfile && test -f /Volumes/SSDX/codes/time/backend/requirements.txt && test -f /Volumes/SSDX/codes/time/backend/.env.example && grep -q "fastapi==0.135.3" /Volumes/SSDX/codes/time/backend/requirements.txt && grep -q "python:3.11-slim" /Volumes/SSDX/codes/time/backend/Dockerfile && grep -q "USER appuser" /Volumes/SSDX/codes/time/backend/Dockerfile && grep -q "pydantic-settings" /Volumes/SSDX/codes/time/backend/requirements.txt && grep -q "redis==5.2.1" /Volumes/SSDX/codes/time/backend/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - File backend/Dockerfile exists
    - backend/Dockerfile contains "FROM python:3.11-slim"
    - backend/Dockerfile contains "USER appuser"
    - backend/Dockerfile contains "EXPOSE 8000"
    - File backend/requirements.txt exists
    - backend/requirements.txt contains "fastapi==0.135.3"
    - backend/requirements.txt contains "sqlalchemy==2.0.48"
    - backend/requirements.txt contains "python-jose[cryptography]==3.5.0"
    - backend/requirements.txt contains "authlib==1.6.9"
    - backend/requirements.txt contains "pydantic-settings==2.7.1"
    - backend/requirements.txt contains "redis==5.2.1"
    - File backend/.env.example exists
    - backend/.env.example contains "DATABASE_URL="
    - backend/.env.example contains "SECRET_KEY="
    - backend/.env.example contains "GOOGLE_CLIENT_ID="
    - backend/.env.example contains "FACEBOOK_CLIENT_ID="
  </acceptance_criteria>
  <done>Backend Dockerfile, requirements.txt, and .env.example created with all required dependencies</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `docker compose config` to validate the compose file syntax
2. Run `docker compose up -d postgres redis` to verify database and Redis services start
3. Run `docker compose ps` to verify service health
</verification>

<success_criteria>
1. Docker Compose configuration is valid (no syntax errors)
2. PostgreSQL and Redis services start successfully
3. Backend and frontend directory structures are in place
4. All environment variable templates are documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
