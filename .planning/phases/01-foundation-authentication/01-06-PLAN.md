---
phase: 01-foundation-authentication
plan: 06
type: execute
wave: 2
depends_on: [01-01]
files_modified: [frontend/package.json, frontend/next.config.js, frontend/tsconfig.json, frontend/tailwind.config.ts, frontend/postcss.config.js]
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - Next.js project is initialized with TypeScript
    - Tailwind CSS is configured
    - Project structure follows API-first architecture (D-16, D-17)
  artifacts:
    - path: frontend/package.json
      provides: "Frontend dependencies and scripts"
      contains: "next@"
    - path: frontend/next.config.js
      provides: "Next.js configuration"
      contains: "module.exports"
    - path: frontend/tsconfig.json
      provides: "TypeScript configuration"
      contains: "compilerOptions"
    - path: frontend/tailwind.config.ts
      provides: "Tailwind CSS configuration"
      contains: "content:"
  key_links:
    - from: frontend/package.json
      to: docker-compose.yml
      via: npm scripts
      pattern: "npm run dev"
    - from: frontend/next.config.js
      to: docker-compose.yml
      via: image building
      pattern: "docker build"
---

<objective>
Initialize the Next.js frontend project with TypeScript, Tailwind CSS, and required dependencies.

Purpose: Set up the frontend foundation following the monorepo structure (INFRA-01, D-16, D-17). This creates the technical base for all frontend UI components including authentication and profile pages.

Output: Initialized Next.js project with TypeScript, Tailwind CSS, and all required dependencies from RESEARCH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend package.json with dependencies</name>
  <files>frontend/package.json</files>
  <read_first>N/A (new file)</read_first>
  <action>
Create frontend/package.json with all required dependencies from RESEARCH.md standard stack:

```json
{
  "name": "notebooksocial-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest",
    "test:unit": "vitest run",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "next": "16.2.2",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "zustand": "5.0.12",
    "zod": "4.3.6",
    "react-hook-form": "7.72.0",
    "@hookform/resolvers": "4.1.3",
    "lucide-react": "0.468.0",
    "clsx": "2.1.1",
    "tailwind-merge": "3.1.0",
    "class-variance-authority": "0.8.1",
    "@radix-ui/react-avatar": "1.2.2",
    "@radix-ui/react-button": "0.1.1",
    "@radix-ui/react-dialog": "1.1.4",
    "@radix-ui/react-dropdown-menu": "2.1.4",
    "@radix-ui/react-label": "2.1.1",
    "@radix-ui/react-slot": "1.1.1",
    "@radix-ui/react-toast": "1.2.4",
    "sonner": "2.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.2",
    "typescript": "^5.8.3",
    "tailwindcss": "4.2.2",
    "postcss": "^8.4.51",
    "autoprefixer": "^10.4.21",
    "eslint": "^9.27.0",
    "eslint-config-next": "16.2.2",
    "vitest": "4.1.2",
    "@testing-library/react": "16.3.2",
    "@testing-library/jest-dom": "6.6.5",
    "@vitejs/plugin-react": "4.4.0",
    "@vitest/ui": "4.1.2"
  }
}
```

Key dependencies per RESEARCH.md:
- Next.js 16.2.2, React 19.2.4 for UI framework
- Zustand 5.0.12 for state management (D-12)
- Zod 4.3.6 for validation (SEC-05)
- React Hook Form 7.72.0 for forms
- Lucide React 0.468.0 for icons
- Radix UI components for shadcn/ui
- Tailwind CSS 4.2.2 for styling
- Vitest 4.1.2 for testing
</action>
  <verify>
    <automated>test -f /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"next": "16.2.2"' /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"react": "19.2.4"' /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"zustand": "5.0.12"' /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"zod": "4.3.6"' /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"tailwindcss": "4.2.2"' /Volumes/SSDX/codes/time/frontend/package.json && grep -q '"vitest": "4.1.2"' /Volumes/SSDX/codes/time/frontend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/package.json exists
    - frontend/package.json contains "\"name\": \"notebooksocial-frontend\""
    - frontend/package.json contains "\"next\": \"16.2.2\""
    - frontend/package.json contains "\"react\": \"19.2.4\""
    - frontend/package.json contains "\"zustand\": \"5.0.12\""
    - frontend/package.json contains "\"zod\": \"4.3.6\""
    - frontend/package.json contains "\"react-hook-form\": \"7.72.0\""
    - frontend/package.json contains "\"tailwindcss\": \"4.2.2\""
    - frontend/package.json contains "\"vitest\": \"4.1.2\""
    - frontend/package.json contains script "dev": "next dev"
    - frontend/package.json contains script "test": "vitest"
  </acceptance_criteria>
  <done>Frontend package.json created with all required dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create Next.js and TypeScript configuration files</name>
  <files>frontend/next.config.js, frontend/tsconfig.json, frontend/.eslintrc.json, frontend/.gitignore</files>
  <read_first>frontend/package.json</read_first>
  <action>
Create Next.js and TypeScript configuration files:

1. frontend/next.config.js:
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api/v1',
    NEXT_PUBLIC_GOOGLE_CLIENT_ID: process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID || '',
    NEXT_PUBLIC_FACEBOOK_CLIENT_ID: process.env.NEXT_PUBLIC_FACEBOOK_CLIENT_ID || '',
  },
}

module.exports = nextConfig
```

2. frontend/tsconfig.json:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"],
      "@/components/*": ["./components/*"],
      "@/lib/*": ["./lib/*"],
      "@/stores/*": ["./stores/*"],
      "@/app/*": ["./app/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

3. frontend/.eslintrc.json:
```json
{
  "extends": "next/core-web-vitals"
}
```

4. frontend/.gitignore (frontend-specific):
```
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# vitest
.vitest
```

5. frontend/next-env.d.ts:
```typescript
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/basic-features/typescript for more information.
```
</action>
  <verify>
    <automated>test -f /Volumes/SSDX/codes/time/frontend/next.config.js && test -f /Volumes/SSDX/codes/time/frontend/tsconfig.json && test -f /Volumes/SSDX/codes/time/frontend/.eslintrc.json && test -f /Volumes/SSDX/codes/time/frontend/.gitignore && grep -q "module.exports" /Volumes/SSDX/codes/time/frontend/next.config.js && grep -q "compilerOptions" /Volumes/SSDX/codes/time/frontend/tsconfig.json && grep -q "\"@/*\"" /Volumes/SSDX/codes/time/frontend/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/next.config.js exists
    - frontend/next.config.js contains "module.exports"
    - frontend/next.config.js contains "NEXT_PUBLIC_API_URL"
    - frontend/next.config.js contains "NEXT_PUBLIC_GOOGLE_CLIENT_ID"
    - File frontend/tsconfig.json exists
    - frontend/tsconfig.json contains "compilerOptions"
    - frontend/tsconfig.json contains "\"@/*\": [\"./*\"]"
    - frontend/tsconfig.json contains "\"strict\": true"
    - File frontend/.eslintrc.json exists
    - File frontend/.gitignore exists
    - frontend/.gitignore contains "/node_modules"
    - frontend/.gitignore contains "/.next/"
  </acceptance_criteria>
  <done>Next.js and TypeScript configuration files created</done>
</task>

<task type="auto">
  <name>Task 3: Create Tailwind CSS and PostCSS configuration</name>
  <files>frontend/tailwind.config.ts, frontend/postcss.config.js, frontend/app/globals.css, frontend/lib/utils.ts</files>
  <read_first>frontend/package.json</read_first>
  <action>
Create Tailwind CSS configuration and utility files:

1. frontend/tailwind.config.ts:
```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [],
};

export default config;
```

2. frontend/postcss.config.js:
```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

3. frontend/app/globals.css:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

4. frontend/lib/utils.ts:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

5. frontend/app/layout.tsx:
```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NotebookSocial - Share and Remix Python Notebooks",
  description: "A social media platform for Python notebooks. Create, share, and fork computational knowledge.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

6. frontend/app/page.tsx:
```typescript
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-background">
      <div className="text-center">
        <h1 className="text-4xl font-bold mb-4">NotebookSocial</h1>
        <p className="text-muted-foreground">
          Share and Remix Python Notebooks
        </p>
      </div>
    </main>
  );
}
```

Key configuration per RESEARCH.md:
- Tailwind CSS 4.2.2 with shadcn/ui variables
- CSS custom properties for theming
- cn utility for class merging
- App Router structure (Next.js 13+)
</action>
  <verify>
    <automated>test -f /Volumes/SSDX/codes/time/frontend/tailwind.config.ts && test -f /Volumes/SSDX/codes/time/frontend/postcss.config.js && test -f /Volumes/SSDX/codes/time/frontend/app/globals.css && test -f /Volumes/SSDX/codes/time/frontend/lib/utils.ts && grep -q "export default config" /Volumes/SSDX/codes/time/frontend/tailwind.config.ts && grep -q "@tailwind base" /Volumes/SSDX/codes/time/frontend/app/globals.css && grep -q "export function cn" /Volumes/SSDX/codes/time/frontend/lib/utils.ts</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/tailwind.config.ts exists
    - frontend/tailwind.config.ts contains "content:"
    - frontend/tailwind.config.ts contains "colors:"
    - File frontend/postcss.config.js exists
    - frontend/postcss.config.js contains "tailwindcss"
    - File frontend/app/globals.css exists
    - frontend/app/globals.css contains "@tailwind base"
    - frontend/app/globals.css contains "@tailwind components"
    - frontend/app/globals.css contains "@tailwind utilities"
    - frontend/app/globals.css contains "--background:"
    - File frontend/lib/utils.ts exists
    - frontend/lib/utils.ts contains "export function cn"
    - File frontend/app/layout.tsx exists
    - File frontend/app/page.tsx exists
  </acceptance_criteria>
  <done>Tailwind CSS and Next.js app structure configured</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `cd frontend && npm install` to install dependencies
2. Run `cd frontend && npm run dev` to start the development server
3. Verify the page loads: `curl http://localhost:3000` should return HTML
4. Verify TypeScript compiles: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
1. All dependencies install successfully
2. Next.js development server starts
3. Tailwind CSS is configured and working
4. TypeScript configuration is valid
5. App Router structure is in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-06-SUMMARY.md`
</output>
