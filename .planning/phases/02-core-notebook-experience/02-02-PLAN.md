---
phase: 02-core-notebook-experience
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/services/notebook_service.py
  - backend/app/services/like_service.py
  - backend/app/services/comment_service.py
  - backend/app/services/feed_service.py
  - backend/app/api/v1/notebooks/router.py
  - backend/app/api/v1/likes/router.py
  - backend/app/api/v1/comments/router.py
  - backend/app/models/__init__.py
autonomous: true
requirements: [NOTE-01, NOTE-02, NOTE-06, NOTE-07, SOC-01, SOC-02, SOC-03, SOC-04, SOC-06, VIEW-01, VIEW-04]

must_haves:
  truths:
    - "NotebookService handles CRUD operations with ownership checks"
    - "LikeService provides toggle endpoint for like/unlike"
    - "CommentService supports threaded comments with depth limit"
    - "FeedService provides cursor-based pagination"
    - "API routers are registered in main.py"
    - "All endpoints use dependency injection for database sessions"
  artifacts:
    - path: backend/app/services/notebook_service.py
      provides: "Notebook CRUD business logic"
      min_lines: 100
      contains: ["class NotebookService", "def create_notebook", "def update_notebook", "def delete_notebook", "def get_notebook"]
    - path: backend/app/services/like_service.py
      provides: "Like/unlike toggle operations"
      min_lines: 50
      contains: ["class LikeService", "def toggle_like"]
    - path: backend/app/services/comment_service.py
      provides: "Threaded comment operations"
      min_lines: 80
      contains: ["class CommentService", "def create_comment", "def get_comment_thread", "WITH RECURSIVE"]
    - path: backend/app/services/feed_service.py
      provides: "Cursor-based feed pagination"
      min_lines: 60
      contains: ["class FeedService", "def get_feed", "cursor"]
    - path: backend/app/api/v1/notebooks/router.py
      provides: "Notebook API endpoints"
      min_lines: 100
      contains: ["@router.post", "@router.get", "@router.put", "@router.delete"]
    - path: backend/app/api/v1/likes/router.py
      provides: "Like/unlike API endpoint"
      min_lines: 40
      contains: ["@router.post", "/toggle"]
    - path: backend/app/api/v1/comments/router.py
      provides: "Comment API endpoints"
      min_lines: 60
      contains: ["@router.post", "/comments", "threaded"]
  key_links:
    - from: backend/app/services/notebook_service.py
      to: backend/app/models/notebook.py
      via: "SQLAlchemy ORM queries using Notebook model"
      pattern: "from app\\.models\\.notebook import Notebook"
    - from: backend/app/services/comment_service.py
      to: backend/app/models/comment.py
      via: "Recursive CTE query for comment threading"
      pattern: "WITH RECURSIVE comment_tree"
    - from: backend/app/api/v1/notebooks/router.py
      to: backend/app/services/notebook_service.py
      via: "Dependency injection of NotebookService"
      pattern: "def.*notebook_service.*NotebookService"
    - from: backend/app/main.py
      to: backend/app/api/v1/notebooks/router.py
      via: "Router inclusion with prefix"
      pattern: "include_router.*notebooks.*prefix"
---

<objective>
Create service layer and API endpoints for notebooks, likes, comments, and feed to provide the backend functionality for Phase 2 features.

Purpose: These services and endpoints handle all business logic for notebook CRUD, social interactions (likes, threaded comments), and feed pagination with cursor-based scrolling. They are consumed by the frontend API client.

Output: Four service classes (NotebookService, LikeService, CommentService, FeedService) and three API routers (notebooks, likes, comments) with FastAPI endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-notebook-experience/02-RESEARCH.md
@backend/app/models/notebook.py
@backend/app/models/notebook_cell.py
@backend/app/models/like.py
@backend/app/models/comment.py
@backend/app/models/user.py
@backend/app/schemas/notebook.py
@backend/app/schemas/like.py
@backend/app/schemas/comment.py
@backend/app/services/profile_service.py
@backend/app/api/v1/profiles/router.py
@backend/app/api/v1/dependencies.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotebookService with CRUD operations and FeedService with cursor pagination</name>
  <files>backend/app/services/notebook_service.py, backend/app/services/feed_service.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/notebook.py (to use Notebook model)
    - backend/app/models/notebook_cell.py (to use NotebookCell model)
    - backend/app/models/user.py (to verify User model exists)
    - backend/app/services/profile_service.py (to understand service patterns and dependency injection)
    - backend/app/schemas/notebook.py (to use schemas)
    - backend/app/api/v1/dependencies.py (to understand get_db dependency)
  </read_first>
  <action>
    Create backend/app/services/notebook_service.py with:
    - Import Notebook, NotebookCell from app.models.notebook and app.models.notebook_cell
    - Import User from app.models.user
    - Import NotebookCreate, NotebookUpdate, NotebookResponse from app.schemas.notebook
    - Import text from sqlalchemy
    - Create NotebookService class with __init__(self, db: Session)
    - Methods:
      * create_notebook(user_id: int, data: NotebookCreate) -> NotebookResponse: Create notebook with title, add initial empty code cell, return response with counts
      * get_notebook(notebook_id: int, db: Session) -> Optional[Notebook]: Get notebook by ID with cells
      * update_notebook(notebook_id: int, user_id: int, data: NotebookUpdate, db: Session) -> NotebookResponse: Update notebook (title or is_published), verify ownership, return response
      * delete_notebook(notebook_id: int, user_id: int, db: Session) -> bool: Delete notebook if owned and not forked (check for existing forks via FK constraint)
      * get_user_notebooks(user_id: int, db: Session, skip: int = 0, limit: int = 20): Get user's notebooks (drafts and published)
      * get_published_notebook_counts(db: Session, notebook_ids: List[int]): Dict[int, dict] with like_count and comment_count
    - Ownership check: verify user_id matches notebook.user_id before update/delete

    Create backend/app/services/feed_service.py with:
    - Import Notebook from app.models.notebook
    - Import User from app.models.user
    - Import func from sqlalchemy
    - Create FeedService class with __init__(self, db: Session)
    - Methods:
      * get_feed(self, cursor: Optional[str] = None, limit: int = 20): Get published notebooks ordered by created_at DESC
      * Cursor-based pagination: Use created_at timestamp as cursor, parse ISO format string to datetime
      * Returns dict with: items (list of notebooks), next_cursor (ISO timestamp or None), has_more (bool)
      * Join with User to get username and avatar_url
      * Only return notebooks where is_published = True
      * Include like_count and comment_count (use subqueries for now, Redis caching in Phase 3)

    Update backend/app/models/__init__.py to export:
    - from app.models.notebook import Notebook
    - from app.models.notebook_cell import NotebookCell
    - from app.models.like import Like
    - from app.models.comment import Comment
  </action>
  <verify>
    <automated>grep -n "class NotebookService" backend/app/services/notebook_service.py | head -5</automated>
    <automated>grep -n "def create_notebook" backend/app/services/notebook_service.py | head -3</automated>
    <automated>grep -n "def update_notebook" backend/app/services/notebook_service.py | head -3</automated>
    <automated>grep -n "class FeedService" backend/app/services/feed_service.py | head -5</automated>
    <automated>grep -n "next_cursor" backend/app/services/feed_service.py | head -3</automated>
    <automated>grep -n "from app.models.notebook import Notebook" backend/app/models/__init__.py | head -3</automated>
  </verify>
  <done>
    NotebookService provides full CRUD operations with ownership checks and cell management. FeedService provides cursor-based pagination for the feed. Models are properly exported from __init__.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LikeService and CommentService with threading support</name>
  <files>backend/app/services/like_service.py, backend/app/services/comment_service.py</files>
  <read_first>
    - backend/app/models/like.py (to use Like model)
    - backend/app/models/comment.py (to use Comment model)
    - backend/app/models/user.py (to verify User model exists)
    - backend/app/schemas/like.py (to use LikeResponse schema)
    - backend/app/schemas/comment.py (to use CommentCreate, CommentResponse schemas)
    - backend/app/api/v1/dependencies.py (to understand get_db dependency)
    - backend/app/services/profile_service.py (to understand service patterns)
  </read_first>
  <action>
    Create backend/app/services/like_service.py with:
    - Import Like from app.models.like
    - Import LikeResponse from app.schemas.like
    - Create LikeService class with __init__(self, db: Session)
    - Methods:
      * toggle_like(self, user_id: int, notebook_id: int, db: Session) -> LikeResponse: Check if like exists, delete if exists (unlike), create if doesn't exist (like), return response
      * get_notebook_likes(self, notebook_id: int, db: Session): Get all likes for a notebook
      * get_user_liked_notebooks(self, user_id: int, db: Session): Get list of notebook_ids user has liked

    Create backend/app/services/comment_service.py with:
    - Import Comment from app.models.comment
    - Import CommentCreate, CommentResponse from app.schemas.comment
    - Import text from sqlalchemy (for recursive CTE)
    - Create CommentService class with __init__(self, db: Session)
    - Methods:
      * create_comment(self, user_id: int, notebook_id: int, content: str, parent_id: Optional[int] = None, db: Session) -> CommentResponse: Create comment, validate depth if parent_id (max 3 levels), return response with username and avatar_url
      * get_comment_thread(self, notebook_id: int, max_depth: int = 3, db: Session) -> List[CommentResponse]: Get threaded comments using recursive CTE query, build tree structure, return root comments with nested replies
      * Recursive CTE query (from RESEARCH.md):
        * WITH RECURSIVE comment_tree AS (SELECT id, user_id, notebook_id, parent_id, content, created_at, 0 as depth, ARRAY[id] as path FROM comments WHERE notebook_id = :notebook_id AND parent_id IS NULL UNION ALL SELECT c.id, c.user_id, c.notebook_id, c.parent_id, c.content, c.created_at, ct.depth + 1 as depth, ct.path || c.id as path FROM comments c JOIN comment_tree ct ON c.parent_id = ct.id WHERE ct.depth < :max_depth) SELECT * FROM comment_tree ORDER BY path
      * Build tree structure in Python: Map comments to parent_id, attach replies to parent comments
      * get_comment_count(self, notebook_id: int, db: Session): Get total comment count for a notebook
  </action>
  <verify>
    <automated>grep -n "class LikeService" backend/app/services/like_service.py | head -5</automated>
    <automated>grep -n "def toggle_like" backend/app/services/like_service.py | head -3</automated>
    <automated>grep -n "class CommentService" backend/app/services/comment_service.py | head -5</automated>
    <automated>grep -n "WITH RECURSIVE" backend/app/services/comment_service.py | head -3</automated>
    <automated>grep -n "max_depth" backend/app/services/comment_service.py | head -3</automated>
  </verify>
  <done>
    LikeService provides toggle endpoint for like/unlike operations. CommentService supports threaded comments with recursive CTE query and depth limit (3 levels). Both services follow Phase 1 service patterns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create API routers for notebooks, likes, and comments with dependency injection</name>
  <files>backend/app/api/v1/notebooks/router.py, backend/app/api/v1/likes/router.py, backend/app/api/v1/comments/router.py, backend/app/main.py</files>
  <read_first>
    - backend/app/services/notebook_service.py (created in Task 1)
    - backend/app/services/like_service.py (created in Task 2)
    - backend/app/services/comment_service.py (created in Task 2)
    - backend/app/services/feed_service.py (created in Task 1)
    - backend/app/schemas/notebook.py (to use schemas)
    - backend/app/schemas/like.py (to use schemas)
    - backend/app/schemas/comment.py (to use schemas)
    - backend/app/api/v1/profiles/router.py (to understand router patterns)
    - backend/app/api/v1/dependencies.py (to use get_current_user dependency)
    - backend/app/main.py (to register routers)
  </read_first>
  <action>
    Create backend/app/api/v1/notebooks/__init__.py with empty file

    Create backend/app/api/v1/notebooks/router.py with:
    - Import FastAPI, Depends, HTTPException, status
    - Import get_current_user from app.api.v1.dependencies
    - Import get_db from app.db.session
    - Import NotebookService, FeedService from app.services (will need to add __init__.py exports)
    - Import NotebookCreate, NotebookUpdate, NotebookResponse from app.schemas.notebook
    - Create router = APIRouter()
    - Endpoints:
      * POST /notebooks: Create notebook, requires authentication, body: NotebookCreate, returns NotebookResponse
      * GET /notebooks/{id}: Get notebook by ID, no auth required (public viewing per AUTH-04), returns NotebookResponse with cells
      * PUT /notebooks/{id}: Update notebook, requires authentication, verify ownership, body: NotebookUpdate, returns NotebookResponse
      * DELETE /notebooks/{id}: Delete notebook, requires authentication, verify ownership, return 204
      * GET /notebooks: Get user's notebooks, requires authentication, returns list of NotebookResponse
      * GET /notebooks/feed: Get feed, no auth required, query param: cursor (optional), returns items, next_cursor, has_more

    Create backend/app/api/v1/likes/__init__.py with empty file

    Create backend/app/api/v1/likes/router.py with:
    - Import FastAPI, Depends, HTTPException, status
    - Import get_current_user from app.api.v1.dependencies
    - Import get_db from app.db.session
    - Import LikeService from app.services
    - Import LikeResponse from app.schemas.like
    - Create router = APIRouter()
    - Endpoints:
      * POST /likes/toggle: Toggle like/unlike, requires authentication, body: {notebook_id: int}, returns LikeResponse

    Create backend/app/api/v1/comments/__init__.py with empty file

    Create backend/app/api/v1/comments/router.py with:
    - Import FastAPI, Depends, HTTPException, status
    - Import get_current_user from app.api.v1.dependencies
    - Import get_db from app.db.session
    - Import CommentService from app.services
    - Import CommentCreate, CommentResponse from app.schemas.comment
    - Create router = APIRouter()
    - Endpoints:
      * POST /comments: Create comment, requires authentication, body: CommentCreate (with notebook_id, parent_id), returns CommentResponse
      * GET /comments/{notebook_id}: Get comment thread, no auth required, returns list of CommentResponse with nested replies
      * GET /comments/{notebook_id}/count: Get comment count, no auth required, returns {count: int}

    Update backend/app/services/__init__.py to export:
    - from app.services.notebook_service import NotebookService
    - from app.services.like_service import LikeService
    - from app.services.comment_service import CommentService
    - from app.services.feed_service import FeedService

    Update backend/app/main.py to include new routers:
    - from app.api.v1.notebooks import router as notebooks_router
    - from app.api.v1.likes import router as likes_router
    - from app.api.v1.comments import router as comments_router
    - app.include_router(notebooks_router, prefix="/api/v1", tags=["notebooks"])
    - app.include_router(likes_router, prefix="/api/v1", tags=["likes"])
    - app.include_router(comments_router, prefix="/api/v1", tags=["comments"])
  </action>
  <verify>
    <automated>grep -n "router = APIRouter()" backend/app/api/v1/notebooks/router.py | head -3</automated>
    <automated>grep -n "@router.post.*notebooks" backend/app/api/v1/notebooks/router.py | head -3</automated>
    <automated>grep -n "@router.get.*feed" backend/app/api/v1/notebooks/router.py | head -3</automated>
    <automated>grep -n "@router.post.*likes" backend/app/api/v1/likes/router.py | head -3</automated>
    <automated>grep -n "@router.post.*comments" backend/app/api/v1/comments/router.py | head -3</automated>
    <automated>grep -n "include_router.*notebooks" backend/app/main.py | head -3</automated>
    <automated>grep -n "include_router.*likes" backend/app/main.py | head -3</automated>
    <automated>grep -n "include_router.*comments" backend/app/main.py | head -3</automated>
  </verify>
  <done>
    All three routers are created with proper endpoints, dependency injection, and authentication checks. Routers are registered in main.py with correct prefixes and tags. Services are exported from __init__.py.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify all service files exist in backend/app/services/
2. Verify all router files exist in backend/app/api/v1/
3. Check main.py includes new routers: grep "include_router" backend/app/main.py
4. Start backend: cd backend && uvicorn app.main:app --reload
5. Test health check: curl http://localhost:8000/health
6. Test docs: visit http://localhost:8000/docs
7. Verify all endpoints appear in OpenAPI docs
</verification>

<success_criteria>
1. All four service classes (NotebookService, LikeService, CommentService, FeedService) exist with correct methods
2. All three routers (notebooks, likes, comments) are created with proper FastAPI endpoints
3. Routers are registered in main.py and appear in OpenAPI docs
4. Dependency injection is used for database sessions and current user
5. CommentService uses recursive CTE for threading
6. FeedService uses cursor-based pagination
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-notebook-experience/02-02-SUMMARY.md`
</output>
