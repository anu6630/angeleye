---
phase: 02-core-notebook-experience
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - frontend/components/feed/FeedList.tsx
  - frontend/components/feed/FeedCard.tsx
  - frontend/components/feed/FeedSkeleton.tsx
  - frontend/app/(public)/feed/page.tsx
  - frontend/app/(public)/layout.tsx
autonomous: true
requirements: [VIEW-01, VIEW-04]

must_haves:
  truths:
    - "Feed displays Instagram-style grid of published notebooks"
    - "Feed loads initial notebooks on page load"
    - "More notebooks load when user scrolls to bottom (infinite scroll)"
    - "Loading skeleton displays while fetching more notebooks"
    - "Error state displays on fetch failure"
    - "Each card shows title, username, avatar, like count, comment count"
    - "Clicking card navigates to notebook viewer"
  artifacts:
    - path: frontend/components/feed/FeedList.tsx
      provides: "Infinite scroll feed container"
      min_lines: 80
      contains: ["useFeedStore", "IntersectionObserver", "loadMore"]
    - path: frontend/components/feed/FeedCard.tsx
      provides: "Individual feed card component"
      min_lines: 60
      contains: ["title", "username", "like_count", "comment_count", "href"]
    - path: frontend/components/feed/FeedSkeleton.tsx
      provides: "Loading skeleton for feed cards"
      min_lines: 40
      contains: ["animate-pulse", "skeleton"]
    - path: frontend/app/(public)/feed/page.tsx
      provides: "Main feed page"
      min_lines: 50
      contains: ["FeedList", "loadFeed"]
  key_links:
    - from: frontend/components/feed/FeedList.tsx
      to: frontend/stores/feed-store.ts
      via: "Zustand hooks for feed state"
      pattern: "useFeedStore"
    - from: frontend/components/feed/FeedList.tsx
      to: frontend/lib/api-client.ts
      via: "API calls for feed data"
      pattern: "apiClient\\.getFeed"
    - from: frontend/components/feed/FeedCard.tsx
      to: frontend/app/(public)/notebooks/[id]/page.tsx
      via: "Link navigation to notebook viewer"
      pattern: "/notebooks/"
---

<objective>
Create Instagram-style feed UI with infinite scroll to display published notebooks (VIEW-01, VIEW-04).

Purpose: The feed is the primary discovery experience for NotebookSocial. Users browse published notebooks in an Instagram-style grid with lazy loading for infinite scroll, seeing social metrics (likes, comments) on each card.

Output: Feed components (FeedList, FeedCard, FeedSkeleton) and main feed page with infinite scroll using Intersection Observer API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-notebook-experience/02-RESEARCH.md
@.planning/phases/02-core-notebook-experience/02-UI-SPEC.md
@frontend/stores/feed-store.ts
@frontend/lib/api-client.ts
@frontend/components/ui/card.tsx
@frontend/components/ui/avatar.tsx
@frontend/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeedCard and FeedSkeleton components</name>
  <files>frontend/components/feed/FeedCard.tsx, frontend/components/feed/FeedSkeleton.tsx</files>
  <read_first>
    - frontend/components/ui/card.tsx (to use Card components)
    - frontend/components/ui/avatar.tsx (to use Avatar component)
    - frontend/lib/api-client.ts (to use NotebookCard interface)
    - frontend/components/profile/ProfileCard.tsx (to see existing card patterns)
  </read_first>
  <action>
    Create frontend/components/feed/FeedCard.tsx with:
    ```typescript
    'use client';

    import Link from 'next/link';
    import { Heart, MessageCircle } from 'lucide-react';
    import { Card, CardHeader, CardContent, CardFooter } from '@/components/ui/card';
    import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar';
    import { NotebookCard } from '@/lib/api-client';

    interface FeedCardProps {
      notebook: NotebookCard;
    }

    export function FeedCard({ notebook }: FeedCardProps) {
      const { id, title, username, avatar_url, like_count, comment_count, created_at } = notebook;

      return (
        <Card className="overflow-hidden hover:shadow-md transition-shadow">
          <Link href={`/notebooks/${id}`}>
            <CardHeader className="pb-3">
              <div className="flex items-center gap-3">
                <Avatar className="h-8 w-8">
                  {avatar_url ? (
                    <AvatarImage src={avatar_url} alt={username} />
                  ) : (
                    <AvatarFallback className="bg-primary text-primary-foreground text-sm">
                      {username.charAt(0).toUpperCase()}
                    </AvatarFallback>
                  )}
                </Avatar>
                <div className="flex-1 min-w-0">
                  <p className="font-medium text-sm truncate">{username}</p>
                  <p className="text-xs text-muted-foreground">
                    {new Date(created_at).toLocaleDateString()}
                  </p>
                </div>
              </div>
            </CardHeader>

            <CardContent className="pb-3">
              <h3 className="font-semibold text-lg line-clamp-2">{title}</h3>
            </CardContent>

            <CardFooter className="flex items-center gap-4 py-3 border-t">
              <div className="flex items-center gap-1 text-sm text-muted-foreground">
                <Heart className="h-4 w-4" />
                <span>{like_count}</span>
              </div>
              <div className="flex items-center gap-1 text-sm text-muted-foreground">
                <MessageCircle className="h-4 w-4" />
                <span>{comment_count}</span>
              </div>
            </CardFooter>
          </Link>
        </Card>
      );
    }
    ```

    Create frontend/components/feed/FeedSkeleton.tsx with:
    ```typescript
    'use client';

    import { Card, CardHeader, CardContent, CardFooter } from '@/components/ui/card';

    interface FeedSkeletonProps {
      count?: number;
    }

    export function FeedSkeleton({ count = 1 }: FeedSkeletonProps) {
      return (
        <>
          {Array.from({ length: count }).map((_, i) => (
            <Card key={i} className="overflow-hidden">
              <CardHeader className="pb-3">
                <div className="flex items-center gap-3">
                  <div className="h-8 w-8 rounded-full bg-muted animate-pulse" />
                  <div className="flex-1 space-y-2">
                    <div className="h-4 w-24 bg-muted animate-pulse rounded" />
                    <div className="h-3 w-16 bg-muted animate-pulse rounded" />
                  </div>
                </div>
              </CardHeader>
              <CardContent className="pb-3">
                <div className="h-6 w-3/4 bg-muted animate-pulse rounded mb-2" />
                <div className="h-6 w-1/2 bg-muted animate-pulse rounded" />
              </CardContent>
              <CardFooter className="flex items-center gap-4 py-3 border-t">
                <div className="h-4 w-8 bg-muted animate-pulse rounded" />
                <div className="h-4 w-8 bg-muted animate-pulse rounded" />
              </CardFooter>
            </Card>
          ))}
        </>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "export function FeedCard" frontend/components/feed/FeedCard.tsx | head -3</automated>
    <automated>grep -n "like_count" frontend/components/feed/FeedCard.tsx | head -3</automated>
    <automated>grep -n "href=.*notebooks" frontend/components/feed/FeedCard.tsx | head -3</automated>
    <automated>grep -n "export function FeedSkeleton" frontend/components/feed/FeedSkeleton.tsx | head -3</automated>
    <automated>grep -n "animate-pulse" frontend/components/feed/FeedSkeleton.tsx | head -3</automated>
  </verify>
  <done>
    FeedCard displays notebook title, author (with avatar), date, like count, and comment count in a card that links to notebook viewer. FeedSkeleton provides loading state with animated pulse effect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FeedList component with infinite scroll using Intersection Observer</name>
  <files>frontend/components/feed/FeedList.tsx</files>
  <read_first>
    - frontend/stores/feed-store.ts (to use useFeedStore)
    - frontend/components/feed/FeedCard.tsx (created in Task 1)
    - frontend/components/feed/FeedSkeleton.tsx (created in Task 1)
    - frontend/components/ui/alert.tsx (to use Alert for errors)
  </read_first>
  <action>
    Create frontend/components/feed/FeedList.tsx with:
    ```typescript
    'use client';

    import { useEffect, useRef, useState } from 'react';
    import { Loader2, RefreshCw } from 'lucide-react';
    import { Button } from '@/components/ui/button';
    import { Alert, AlertDescription } from '@/components/ui/alert';
    import { FeedCard } from './FeedCard';
    import { FeedSkeleton } from './FeedSkeleton';
    import { useFeedStore } from '@/stores';

    export function FeedList() {
      const {
        notebooks,
        isLoading,
        hasMore,
        error,
        loadFeed,
        loadMore,
      } = useFeedStore();

      const observerTarget = useRef<HTMLDivElement>(null);
      const [isIntersecting, setIsIntersecting] = useState(false);

      // Initial load
      useEffect(() => {
        loadFeed();
      }, [loadFeed]);

      // Intersection Observer for infinite scroll
      useEffect(() => {
        const observer = new IntersectionObserver(
          (entries) => {
            const [entry] = entries;
            setIsIntersecting(entry.isIntersecting);
          },
          { threshold: 0.1 }
        );

        const target = observerTarget.current;
        if (target) {
          observer.observe(target);
        }

        return () => {
          if (target) {
            observer.unobserve(target);
          }
        };
      }, []);

      // Load more when intersecting and conditions met
      useEffect(() => {
        if (isIntersecting && hasMore && !isLoading) {
          loadMore();
        }
      }, [isIntersecting, hasMore, isLoading, loadMore]);

      const handleRetry = () => {
        loadFeed();
      };

      if (error) {
        return (
          <div className="container mx-auto py-8 px-4 max-w-6xl">
            <Alert variant="destructive">
              <AlertDescription className="flex items-center justify-between">
                <span>Failed to load feed: {error}</span>
                <Button variant="outline" size="sm" onClick={handleRetry}>
                  <RefreshCw className="h-4 w-4 mr-2" />
                  Retry
                </Button>
              </AlertDescription>
            </Alert>
          </div>
        );
      }

      return (
        <div className="container mx-auto py-8 px-4 max-w-6xl">
          {/* Empty state */}
          {notebooks.length === 0 && !isLoading && (
            <div className="text-center py-12">
              <h2 className="text-xl font-semibold mb-2">No notebooks yet</h2>
              <p className="text-muted-foreground">
                Be the first to publish a notebook!
              </p>
            </div>
          )}

          {/* Feed grid - Instagram style (3 columns on lg, 2 on md, 1 on sm) */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {notebooks.map((notebook) => (
              <FeedCard key={notebook.id} notebook={notebook} />
            ))}
          </div>

          {/* Loading skeleton for initial load */}
          {isLoading && notebooks.length === 0 && <FeedSkeleton count={6} />}

          {/* Loading spinner for loading more */}
          {isLoading && notebooks.length > 0 && (
            <div className="flex justify-center py-8">
              <Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />
            </div>
          )}

          {/* Intersection Observer target at bottom */}
          <div ref={observerTarget} className="h-4" />

          {/* End of feed indicator */}
          {!hasMore && notebooks.length > 0 && !isLoading && (
            <div className="text-center py-8 text-muted-foreground">
              <p>You've seen all notebooks</p>
            </div>
          )}
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "export function FeedList" frontend/components/feed/FeedList.tsx | head -3</automated>
    <automated>grep -n "IntersectionObserver" frontend/components/feed/FeedList.tsx | head -3</automated>
    <automated>grep -n "loadMore" frontend/components/feed/FeedList.tsx | head -3</automated>
    <automated>grep -n "grid-cols-3" frontend/components/feed/FeedList.tsx | head -3</automated>
  </verify>
  <done>
    FeedList implements infinite scroll using Intersection Observer API. Displays feed in 3-column grid (responsive), shows skeleton on initial load, spinner on loading more, error state with retry, and end-of-feed indicator.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create public layout and feed page</name>
  <files>frontend/app/(public)/layout.tsx, frontend/app/(public)/feed/page.tsx</files>
  <read_first>
    - frontend/app/layout.tsx (to understand root layout)
    - frontend/components/feed/FeedList.tsx (created in Task 2)
  </read_first>
  <action>
    Create frontend/app/(public)/layout.tsx with:
    ```typescript
    import type { Metadata } from "next";
    import { Inter } from "next/font/google";
      from "@/components/ui/button";

    const inter = Inter({ subsets: ["latin"] });

    export const metadata: Metadata = {
      title: "NotebookSocial - Feed",
      description: "Discover Python notebooks from the community",
    };

    export default function PublicLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <html lang="en">
          <body className={inter.className}>
            {/* Header for public pages */}
            <header className="sticky top-0 z-50 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 border-b">
              <div className="container mx-auto px-4 h-16 flex items-center justify-between">
                <Link href="/feed" className="flex items-center gap-2">
                  <span className="text-xl font-bold">NotebookSocial</span>
                </Link>

                <nav className="flex items-center gap-4">
                  <Link href="/feed">
                    <Button variant="ghost" size="sm">
                      Feed
                    </Button>
                  </Link>
                  <Link href="/my-notebooks">
                    <Button variant="ghost" size="sm">
                      My Notebooks
                    </Button>
                  </Link>
                  <Link href="/notebooks/new">
                    <Button size="sm">
                      Create Notebook
                    </Button>
                  </Link>
                  {/* Auth state handled via auth-store for login/logout */}
                </nav>
              </div>
            </header>

            {children}
          </body>
        </html>
      );
    }
    ```

    Create frontend/app/(public)/feed/page.tsx with:
    ```typescript
    'use client';

    import { FeedList } from '@/components/feed/FeedList';
    import { useAuthStore } from '@/stores';

    export default function FeedPage() {
      const { user, isAuthenticated } = useAuthStore();

      return (
        <main className="min-h-screen bg-background">
          <FeedList />
        </main>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "export default function PublicLayout" frontend/app/(public)/layout.tsx | head -3</automated>
    <automated>grep -n "export default function FeedPage" frontend/app/(public)/feed/page.tsx | head -3</automated>
    <automated>grep -n "FeedList" frontend/app/(public)/feed/page.tsx | head -3</automated>
    <automated>grep -n "href=.*/feed" frontend/app/(public)/layout.tsx | head -3</automated>
  </verify>
  <done>
    Public layout provides header with navigation (Feed, My Notebooks, Create Notebook). Feed page displays FeedList component. No authentication required for feed viewing (AUTH-04).
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify components exist: ls frontend/components/feed/*.tsx
2. Verify feed page exists: ls frontend/app/(public)/feed/page.tsx
3. Check for TypeScript errors: cd frontend && npx tsc --noEmit
4. Test navigation: cd frontend && npm run dev, visit http://localhost:3000/feed
</verification>

<success_criteria>
1. FeedCard displays notebook info with author, date, likes, comments
2. FeedSkeleton provides loading state
3. FeedList implements infinite scroll with Intersection Observer
4. Feed page displays FeedList
5. Public layout provides navigation header
6. Grid layout is 3 columns on desktop, 2 on tablet, 1 on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-notebook-experience/02-05-SUMMARY.md`
</output>
