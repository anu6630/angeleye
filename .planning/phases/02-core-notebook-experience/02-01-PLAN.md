---
phase: 02-core-notebook-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/notebook.py
  - backend/app/models/notebook_cell.py
  - backend/app/models/like.py
  - backend/app/models/comment.py
  - backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py
  - backend/app/schemas/notebook.py
  - backend/app/schemas/like.py
  - backend/app/schemas/comment.py
autonomous: true
requirements: [NOTE-01, NOTE-02, NOTE-06, NOTE-07, SOC-01, SOC-02, SOC-03, SOC-04, SOC-06, PERF-03]

must_haves:
  truths:
    - "Notebook model exists with title, user_id, is_published fields"
    - "NotebookCell model exists for storing individual notebook cells with cell_type and content"
    - "Like model exists with unique constraint on (user_id, notebook_id)"
    - "Comment model exists with parent_id for threaded replies and depth limit"
    - "Database migration creates all tables with proper indexes and constraints"
    - "Pydantic schemas exist for request/response validation"
  artifacts:
    - path: backend/app/models/notebook.py
      provides: "Notebook SQLAlchemy model"
      min_lines: 40
      contains: ["class Notebook", "user_id", "is_published", "created_at"]
    - path: backend/app/models/notebook_cell.py
      provides: "NotebookCell SQLAlchemy model for individual cells"
      min_lines: 30
      contains: ["class NotebookCell", "notebook_id", "cell_type", "content"]
    - path: backend/app/models/like.py
      provides: "Like SQLAlchemy model with unique constraint"
      min_lines: 25
      contains: ["class Like", "user_id", "notebook_id", "unique_together"]
    - path: backend/app/models/comment.py
      provides: "Comment SQLAlchemy model for threaded replies"
      min_lines: 35
      contains: ["class Comment", "parent_id", "depth"]
    - path: backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py
      provides: "Alembic migration for notebook social tables"
      min_lines: 100
      contains: ["create_table('notebooks'", "create_table('notebook_cells'", "create_table('likes'", "create_table('comments'"]
    - path: backend/app/schemas/notebook.py
      provides: "Pydantic schemas for notebook operations"
      min_lines: 40
      contains: ["class NotebookCreate", "class NotebookResponse", "class NotebookCell"]
    - path: backend/app/schemas/like.py
      provides: "Pydantic schemas for like operations"
      min_lines: 20
      contains: ["class LikeResponse"]
    - path: backend/app/schemas/comment.py
      provides: "Pydantic schemas for comment operations"
      min_lines: 30
      contains: ["class CommentCreate", "class CommentResponse"]
  key_links:
    - from: backend/app/models/notebook.py
      to: backend/app/models/user.py
      via: "Foreign key on user_id referencing users.id"
      pattern: "ForeignKey\\('users\\.id'"
    - from: backend/app/models/comment.py
      to: backend/app/models/comment.py
      via: "Self-referential foreign key on parent_id for threading"
      pattern: "parent_id.*ForeignKey.*'comments\\.id'"
    - from: backend/app/models/notebook_cell.py
      to: backend/app/models/notebook.py
      via: "Foreign key on notebook_id with cascade delete"
      pattern: "notebook_id.*ForeignKey.*'notebooks\\.id'.*ondelete.*CASCADE"
---

<objective>
Create database models and migrations for notebooks, cells, likes, and comments to establish the data foundation for Phase 2 features.

Purpose: These models are the core data structures that support notebook creation, viewing, and all social interactions (likes, threaded comments). Without this foundation, no API endpoints or UI components can be built.

Output: Four SQLAlchemy models (Notebook, NotebookCell, Like, Comment) with proper relationships, indexes, and a migration file to create the database tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-core-notebook-experience/02-RESEARCH.md
@backend/app/models/user.py
@backend/app/models/profile.py
@backend/app/db/base.py
@backend/app/schemas/user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Notebook and NotebookCell SQLAlchemy models</name>
  <files>backend/app/models/notebook.py, backend/app/models/notebook_cell.py</files>
  <read_first>
    - backend/app/models/user.py (to understand existing model patterns and relationships)
    - backend/app/models/profile.py (to understand cascade delete patterns)
    - backend/app/db/base.py (to import Base class)
  </read_first>
  <action>
    Create backend/app/models/notebook.py with:
    - Import Base from app.db.base
    - Import Column, Integer, String, Text, Boolean, DateTime, ForeignKey from sqlalchemy
    - Import relationship from sqlalchemy.orm
    - Import func from sqlalchemy
    - Create Notebook class with __tablename__ = "notebooks"
    - Fields:
      * id: Integer, primary_key=True, index=True
      * title: String(255), nullable=False
      * user_id: Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True
      * is_published: Boolean, default=False, nullable=False, index=True
      * created_at: DateTime(timezone=True), server_default=func.now(), nullable=False, index=True
      * updated_at: DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False
    - Relationship to User: user = relationship("User", back_populates="notebooks")
    - Relationship to NotebookCell: cells = relationship("NotebookCell", back_populates="notebook", cascade="all, delete-orphan")
    - Relationship to Like: likes = relationship("Like", back_populates="notebook", cascade="all, delete-orphan")
    - Relationship to Comment: comments = relationship("Comment", back_populates="notebook", cascade="all, delete-orphan")

    Create backend/app/models/notebook_cell.py with:
    - Import Base from app.db.base
    - Import Column, Integer, String, Text, ForeignKey from sqlalchemy
    - Import relationship from sqlalchemy.orm
    - Import func from sqlalchemy
    - Create NotebookCell class with __tablename__ = "notebook_cells"
    - Fields:
      * id: Integer, primary_key=True, index=True
      * notebook_id: Integer, ForeignKey("notebooks.id", ondelete="CASCADE"), nullable=False, index=True
      * cell_type: String(50), nullable=False (values: "code", "markdown")
      * content: Text, nullable=False
      * order_index: Integer, nullable=False (for cell ordering)
      * created_at: DateTime(timezone=True), server_default=func.now(), nullable=False
      * updated_at: DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False
    - Relationship to Notebook: notebook = relationship("Notebook", back_populates="cells")
  </action>
  <verify>
    <automated>grep -n "class Notebook" backend/app/models/notebook.py | head -5</automated>
    <automated>grep -n "is_published" backend/app/models/notebook.py | head -3</automated>
    <automated>grep -n "class NotebookCell" backend/app/models/notebook_cell.py | head -5</automated>
  </verify>
  <done>
    Notebook model exists with all required fields and relationships to User, NotebookCell, Like, and Comment. NotebookCell model exists with cell_type, content, and order_index fields. Both models import Base correctly and follow existing patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Like and Comment SQLAlchemy models with threading support</name>
  <files>backend/app/models/like.py, backend/app/models/comment.py</files>
  <read_first>
    - backend/app/models/user.py (to understand User relationship patterns)
    - backend/app/models/notebook.py (created in previous task, to reference Notebook)
    - backend/app/db/base.py (to import Base class)
  </read_first>
  <action>
    Create backend/app/models/like.py with:
    - Import Base from app.db.base
    - Import Column, Integer, ForeignKey, UniqueConstraint from sqlalchemy
    - Import relationship from sqlalchemy.orm
    - Import func from sqlalchemy
    - Create Like class with __tablename__ = "likes"
    - Fields:
      * id: Integer, primary_key=True, index=True
      * user_id: Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True
      * notebook_id: Integer, ForeignKey("notebooks.id", ondelete="CASCADE"), nullable=False, index=True
      * created_at: DateTime(timezone=True), server_default=func.now(), nullable=False
    - UniqueConstraint on (user_id, notebook_id) to prevent duplicate likes
    - Relationship to User: user = relationship("User", back_populates="likes")
    - Relationship to Notebook: notebook = relationship("Notebook", back_populates="likes")

    Create backend/app/models/comment.py with:
    - Import Base from app.db.base
    - Import Column, Integer, String, Text, ForeignKey from sqlalchemy
    - Import relationship from sqlalchemy.orm
    - Import func from sqlalchemy
    - Create Comment class with __tablename__ = "comments"
    - Fields:
      * id: Integer, primary_key=True, index=True
      * notebook_id: Integer, ForeignKey("notebooks.id", ondelete="CASCADE"), nullable=False, index=True
      * user_id: Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True
      * parent_id: Integer, ForeignKey("comments.id", ondelete="CASCADE"), nullable=True, index=True
      * content: Text, nullable=False
      * created_at: DateTime(timezone=True), server_default=func.now(), nullable=False, index=True
      * updated_at: DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False
    - Self-referential relationship: parent = relationship("Comment", remote_side=[id], backref="replies")
    - Relationship to User: user = relationship("User", back_populates="comments")
    - Relationship to Notebook: notebook = relationship("Notebook", back_populates="comments")
    - Note: Depth limit will be enforced in service layer (max 3 levels)
  </action>
  <verify>
    <automated>grep -n "class Like" backend/app/models/like.py | head -5</automated>
    <automated>grep -n "UniqueConstraint" backend/app/models/like.py | head -3</automated>
    <automated>grep -n "class Comment" backend/app/models/comment.py | head -5</automated>
    <automated>grep -n "parent_id" backend/app/models/comment.py | head -5</automated>
  </verify>
  <done>
    Like model exists with unique constraint preventing duplicate likes. Comment model exists with parent_id for threaded replies and self-referential relationship. Both models have proper cascade deletes and indexes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Alembic migration for notebook social tables and Pydantic schemas</name>
  <files>backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py, backend/app/schemas/notebook.py, backend/app/schemas/like.py, backend/app/schemas/comment.py</files>
  <read_first>
    - backend/alembic/versions/20260403_0400-001_initial_schema.py (to understand migration pattern)
    - backend/app/models/notebook.py (created in Task 1)
    - backend/app/models/notebook_cell.py (created in Task 1)
    - backend/app/models/like.py (created in Task 2)
    - backend/app/models/comment.py (created in Task 2)
    - backend/app/schemas/user.py (to understand Pydantic schema patterns)
  </read_first>
  <action>
    Create backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py with:
    - Revision ID: '002', down_revision: '001'
    - upgrade() function that creates:
      * notebooks table with all fields and indexes
      * notebook_cells table with foreign key and cascade delete
      * likes table with unique constraint on (user_id, notebook_id)
      * comments table with parent_id for threading
      * Indexes on: user_id, notebook_id, is_published, created_at for notebooks; notebook_id for cells; user_id, notebook_id for likes; notebook_id, user_id, parent_id, created_at for comments
    - downgrade() function that drops tables in reverse order (comments, likes, notebook_cells, notebooks)

    Create backend/app/schemas/notebook.py with:
    - Import BaseModel, Field from pydantic
    - Create NotebookCell schema: id (int), cell_type (str), content (str), order_index (int)
    - Create NotebookCreate schema: title (str, Field(min_length=1, max_length=255))
    - Create NotebookUpdate schema: title (str, Field(min_length=1, max_length=255)), is_published (bool)
    - Create NotebookResponse schema with:
      * id (int), title (str), user_id (int), is_published (bool)
      * created_at (datetime), updated_at (datetime)
      * like_count (int, default=0), comment_count (int, default=0)
      * cells (Optional[List[NotebookCell]])
    - Create NotebookListResponse schema for feed items: id, title, username, avatar_url, like_count, comment_count, created_at

    Create backend/app/schemas/like.py with:
    - Import BaseModel from pydantic
    - Create LikeResponse schema: id (int), notebook_id (int), user_id (int), created_at (datetime)

    Create backend/app/schemas/comment.py with:
    - Import BaseModel from pydantic
    - Create CommentCreate schema: content (str, Field(min_length=1, max_length=2000))
    - Create CommentResponse schema with:
      * id (int), notebook_id (int), user_id (int), parent_id (Optional[int])
      * content (str), created_at (datetime), updated_at (datetime)
      * username (str), avatar_url (Optional[str])
      * replies (Optional[List[CommentResponse]])
  </action>
  <verify>
    <automated>grep -n "revision = '002'" backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py | head -3</automated>
    <automated>grep -n "create_table('notebooks'" backend/alembic/versions/20260404_1000-002_add_notebook_social_tables.py | head -3</automated>
    <automated>grep -n "class NotebookResponse" backend/app/schemas/notebook.py | head -5</automated>
    <automated>grep -n "like_count" backend/app/schemas/notebook.py | head -3</automated>
    <automated>grep -n "class CommentResponse" backend/app/schemas/comment.py | head -5</automated>
  </verify>
  <done>
    Migration file creates all four tables with proper indexes and constraints. Pydantic schemas define request/response contracts for notebooks, likes, and comments including nested structures for cells and threaded comments.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify all model files exist in backend/app/models/
2. Run migration: cd backend && alembic upgrade head
3. Verify tables exist: psql -U notebooksocial -d notebooksocial -c "\dt"
4. Verify notebooks table schema: psql -U notebooksocial -d notebooksocial -c "\d notebooks"
5. Verify unique constraint on likes: psql -U notebooksocial -d notebooksocial -c "\d likes"
</verification>

<success_criteria>
1. All four SQLAlchemy models (Notebook, NotebookCell, Like, Comment) exist with correct fields and relationships
2. Alembic migration runs successfully and creates all tables
3. Pydantic schemas exist for all CRUD operations
4. Models follow Phase 1 patterns (cascade deletes, proper indexes, datetime fields with server_default)
5. Comment model supports threading via parent_id
6. Like model has unique constraint to prevent duplicates
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-notebook-experience/02-01-SUMMARY.md`
</output>
