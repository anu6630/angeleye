---
phase: 05-testing-quality
plan: 03
type: execute
wave: 3
depends_on: [05-01]
files_modified: ["backend/tests/integration/*.py"]
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Integration tests cover all 6 major user flows"
    - "Tests use real PostgreSQL and Redis (mocked OAuth only)"
    - "Multi-endpoint workflows verify end-to-end behavior"
    - "Database transactions rolled back after each test"
    - "Tests catch mock drift from actual service behavior"
  artifacts:
    - path: "backend/tests/integration/test_auth_flow.py"
      provides: "Authentication flow integration tests"
      contains: "OAuth callback → user creation → session"
    - path: "backend/tests/integration/test_notebook_lifecycle.py"
      provides: "Notebook lifecycle integration tests"
      contains: "create → edit → compile → publish → view"
    - path: "backend/tests/integration/test_forking_flow.py"
      provides: "Forking integration tests"
      contains: "fork → verify lineage → independent edits"
    - path: "backend/tests/integration/test_social_flow.py"
      provides: "Social interactions integration tests"
      contains: "follow → like → comment → feed updates"
    - path: "backend/tests/integration/test_search_flow.py"
      provides: "Search integration tests"
      contains: "publish → index → search → filter"
    - path: "backend/tests/integration/test_feed_flow.py"
      provides: "Feed integration tests"
      contains: "followed users → personalized feed → trending fallback"
  key_links:
    - from: "integration tests"
      to: "real PostgreSQL"
      via: "docker-compose test services"
      pattern: "postgres://test_user@test_db/test_db"
    - from: "integration tests"
      to: "real Redis"
      via: "docker-compose test services"
      pattern: "redis://test_redis:6379/1"
---

<objective>
Create integration tests covering all 6 major user flows with real PostgreSQL and Redis

Purpose: Verify multi-endpoint workflows work correctly together, catching issues that unit tests miss. Integration tests use real database and cache per D-05 (smoke tests to catch mock drift), but still mock OAuth (external service).

Output: 6 integration test files covering authentication, notebook lifecycle, forking, social interactions, search, and feed flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-quality/05-CONTEXT.md
@.planning/phases/05-testing-quality/05-RESEARCH.md
@backend/tests/conftest.py
@backend/tests/test_factories.py

# Locked Decisions from CONTEXT.md
- D-05: With aggressive mocking, add 2-3 smoke tests using real PostgreSQL and Redis to catch mock drift
- D-11: Comprehensive integration tests covering 6 user flows

# 6 User Flows from D-11
1. Authentication: OAuth callback → user creation → session
2. Notebook lifecycle: create → edit → compile → publish → view
3. Forking: fork → verify lineage → independent edits
4. Social interactions: follow → like → comment → feed updates
5. Search: publish → index → search → filter
6. Feed: followed users → personalized feed → trending fallback

# Integration Test Patterns
- Use FastAPI TestClient for HTTP requests
- Use real PostgreSQL in Docker for database
- Use real Redis in Docker for cache
- Rollback transactions after each test (fast cleanup)
- Mock only external APIs (OAuth providers, S3 for uploads)
- Verify state changes across multiple endpoints
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test for authentication flow</name>
  <files>backend/tests/integration/test_auth_flow.py</files>
  <action>
    Create backend/tests/integration/test_auth_flow.py:

    Test: OAuth callback creates new user
    - Mock authlib OAuth response (Google user)
    - POST /api/v1/auth/google/callback with oauth_token
    - Verify new user created in database
    - Verify user has correct oauth_provider and oauth_id
    - Verify response contains user object
    - Verify session cookie set (httpOnly)

    Test: OAuth callback logs in existing user
    - Create user in database via factory
    - Mock OAuth response returning same oauth_id
    - POST /api/v1/auth/google/callback
    - Verify existing user returned (not duplicate)
    - Verify session cookie set

    Test: Get current user with valid session
    - Create user and session
    - GET /api/v1/auth/me with session cookie
    - Verify user object returned

    Test: Get current user with invalid session
    - GET /api/v1/auth/me with invalid token
    - Verify 401 Unauthorized

    Test: Logout clears session
    - Create user and session
    - POST /api/v1/auth/logout
    - Verify session cookie cleared
    - Verify GET /api/v1/auth/me returns 401

    Use @pytest.mark.integration marker
    Use real PostgreSQL via db_session fixture (already in conftest.py)
    Mock authlib OAuth (external API)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_auth_flow.py -v -m integration</automated>
  </verify>
  <done>Authentication flow integration tests verify OAuth, user creation, and session management</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test for notebook lifecycle</name>
  <files>backend/tests/integration/test_notebook_lifecycle.py</files>
  <action>
    Create backend/tests/integration/test_notebook_lifecycle.py:

    Test: Create notebook and add cells
    - Create authenticated user
    - POST /api/v1/notebooks with title and description
    - Verify notebook created (201)
    - POST /api/v1/notebooks/{id}/cells with code cell
    - POST /api/v1/notebooks/{id}/cells with markdown cell
    - GET /api/v1/notebooks/{id}
    - Verify notebook has 2 cells in correct order

    Test: Edit notebook cells
    - Create notebook with cells
    - PUT /api/v1/notebooks/{id}/cells/{cell_id} with new content
    - GET /api/v1/notebooks/{id}
    - Verify cell content updated

    Test: Compile notebook (mock container execution)
    - Create notebook with code cells
    - Mock ContainerExecutor (return success)
    - Mock StorageService (S3 upload)
    - POST /api/v1/notebooks/{id}/compile
    - Verify status = 'compiling'
    - Poll GET /api/v1/notebooks/{id}/compilation-status
    - Verify status changes to 'success'
    - Verify output_url set

    Test: Publish notebook
    - Create compiled notebook
    - POST /api/v1/notebooks/{id}/publish
    - Verify is_published = True
    - Verify published_at set
    - Verify notebook appears in feed

    Test: View published notebook
    - GET /api/v1/feed
    - Verify published notebook in feed
    - GET /api/v1/notebooks/{id}
    - Verify output accessible

    Test: Delete draft notebook
    - Create draft notebook
    - DELETE /api/v1/notebooks/{id}
    - Verify 204 No Content
    - GET /api/v1/notebooks/{id}
    - Verify 404 Not Found

    Test: Cannot delete published notebook with forks
    - Create published notebook
    - Create fork
    - DELETE /api/v1/notebooks/{parent_id}
    - Verify 403 Forbidden

    Use real PostgreSQL for database operations
    Mock ContainerExecutor and S3 (external services)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_notebook_lifecycle.py -v -m integration</automated>
  </verify>
  <done>Notebook lifecycle integration tests verify create, edit, compile, publish, view workflow</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test for forking flow</name>
  <files>backend/tests/integration/test_forking_flow.py</files>
  <action>
    Create backend/tests/integration/test_forking_flow.py:

    Test: Fork notebook
    - Create user A and published notebook
    - Create user B
    - Authenticate as user B
    - POST /api/v1/notebooks/{id}/fork
    - Verify fork created (201)
    - Verify fork.parent_id = original notebook
    - Verify fork.root_id = original notebook
    - Verify fork.user_id = user B

    Test: Fork copies cells
    - Create notebook with 3 cells
    - Fork notebook
    - GET /api/v1/notebooks/{fork_id}
    - Verify fork has 3 cells
    - Verify cell content identical
    - Verify cell order preserved

    Test: Fork chain (grandchild)
    - Create original notebook
    - Create fork A
    - Create fork B from fork A
    - Verify fork A.parent_id = original
    - Verify fork A.root_id = original
    - Verify fork B.parent_id = fork A
    - Verify fork B.root_id = original (root preserved)

    Test: Independent edits
    - Create notebook and fork
    - Add cell to original
    - Add cell to fork
    - GET /api/v1/notebooks/{original_id}
    - Verify original has new cell
    - GET /api/v1/notebooks/{fork_id}
    - Verify fork has different new cell
    - Verify edits independent

    Test: Fork attribution
    - GET /api/v1/notebooks/{fork_id}/lineage
    - Verify lineage returns [original, fork]
    - Verify each step has user info

    Test: Delete protection
    - Create original notebook
    - Create fork
    - DELETE /api/v1/notebooks/{original_id}
    - Verify 403 Forbidden (has forks)
    - DELETE /api/v1/notebooks/{fork_id}
    - Verify 204 No Content

    Test: Forks in feed
    - Create original notebook
    - Create fork
    - GET /api/v1/feed
    - Verify both original and fork in feed
    - Verify both have equal weightage

    Use real PostgreSQL for database
    Test fork relationships via queries
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_forking_flow.py -v -m integration</automated>
  </verify>
  <done>Forking integration tests verify fork creation, lineage tracking, independent edits, and delete protection</done>
</task>

<task type="auto">
  <name>Task 4: Create integration test for social interactions flow</name>
  <files>backend/tests/integration/test_social_flow.py</files>
  <action>
    Create backend/tests/integration/test_social_flow.py:

    Test: Follow user
    - Create user A and user B
    - Authenticate as user A
    - POST /api/v1/follows with following_id = user B
    - Verify follow created (201)
    - GET /api/v1/following/{user_a_id}
    - Verify user B in following list
    - GET /api/v1/followers/{user_b_id}
    - Verify user A in followers list

    Test: Unfollow user
    - Create follow relationship
    - DELETE /api/v1/follows/{follow_id}
    - Verify 204 No Content
    - GET /api/v1/following/{user_a_id}
    - Verify user B not in following

    Test: Like notebook
    - Create user and notebook
    - POST /api/v1/likes with notebook_id
    - Verify like created (201)
    - GET /api/v1/notebooks/{id}
    - Verify like_count = 1

    Test: Unlike notebook
    - Create like
    - POST /api/v1/likes with notebook_id (toggle)
    - Verify like removed
    - GET /api/v1/notebooks/{id}
    - Verify like_count = 0

    Test: Comment on notebook
    - Create user and notebook
    - POST /api/v1/comments with content and notebook_id
    - Verify comment created (201)
    - GET /api/v1/comments?notebook_id={id}
    - Verify comment in list
    - GET /api/v1/notebooks/{id}
    - Verify comment_count = 1

    Test: Reply to comment
    - Create parent comment
    - POST /api/v1/comments with content and parent_id
    - Verify reply created
    - GET /api/v1/comments?notebook_id={id}
    - Verify reply nested under parent

    Test: Social engagement updates trending
    - Create notebook
    - Create 10 likes and 5 comments via multiple users
    - Wait for Celery beat task (or call manually)
    - GET /api/v1/feed/trending
    - Verify notebook appears in trending (high engagement score)

    Test: Feed personalization
    - Create user A (follower)
    - Create user B and C (followed)
    - Create notebooks by user B and user C
    - User A follows user B
    - GET /api/v1/feed (as user A)
    - Verify feed includes user B's notebooks
    - Verify feed excludes user C's notebooks (not followed)
    - Verify feed includes trending fallback (if followed content insufficient)

    Use real PostgreSQL and Redis
    Mock Celery for async tasks (call tasks directly)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_social_flow.py -v -m integration</automated>
  </verify>
  <done>Social interactions integration tests verify follow, like, comment, feed updates, and trending</done>
</task>

<task type="auto">
  <name>Task 5: Create integration test for search flow</name>
  <files>backend/tests/integration/test_search_flow.py</files>
  <action>
    Create backend/tests/integration/test_search_flow.py:

    Test: Publish notebook indexes to search
    - Create user and notebook with tags ['python', 'data']
    - Mock Meilisearch index_document
    - POST /api/v1/notebooks/{id}/publish
    - Verify Meilisearch index called with correct fields
    - Verify tags included in index

    Test: Search by title
    - Create notebooks with titles 'Data Analysis' and 'ML Models'
    - Mock Meilisearch search to return 'Data Analysis'
    - GET /api/v1/search?q=Data
    - Verify 'Data Analysis' in results
    - Verify 'ML Models' not in results

    Test: Search by tags
    - Create notebook with tags ['python', 'ml']
    - Mock Meilisearch search with tag filter
    - GET /api/v1/search?q=&tags=python
    - Verify notebook in results

    Test: Filter by fork_type
    - Create original notebook
    - Create fork
    - Mock Meilisearch search
    - GET /api/v1/search?fork_type=original
    - Verify only original in results
    - GET /api/v1/search?fork_type=fork
    - Verify only fork in results
    - GET /api/v1/search?fork_type=all
    - Verify both in results

    Test: Search by author
    - Create user A and user B
    - Create notebooks by each user
    - Mock Meilisearch search with author filter
    - GET /api/v1/search?author={user_a_username}
    - Verify only user A's notebooks in results

    Test: Update search index
    - Create and publish notebook
    - Update notebook title
    - Mock Meilisearch update_document
    - PUT /api/v1/notebooks/{id}
    - Verify Meilisearch index updated

    Test: Delete from search index
    - Create and publish notebook
    - Mock Meilisearch delete_document
    - DELETE /api/v1/notebooks/{id}
    - Verify Meilisearch document deleted

    Use real PostgreSQL for notebook queries
    Mock Meilisearch (external service, but verify contract)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_search_flow.py -v -m integration</automated>
  </verify>
  <done>Search integration tests verify indexing, search queries, and filters</done>
</task>

<task type="auto">
  <name>Task 6: Create integration test for feed flow</name>
  <files>backend/tests/integration/test_feed_flow.py</files>
  <action>
    Create backend/tests/integration/test_feed_flow.py:

    Test: Personalized feed from follows
    - Create user A (follower)
    - Create user B, C, D (followed by A)
    - User A follows user B and user C
    - Create notebooks by user B, C, D
    - GET /api/v1/feed (as user A)
    - Verify feed includes user B's and user C's notebooks
    - Verify feed excludes user D's notebooks
    - Verify feed sorted by created_at

    Test: Feed pagination with cursor
    - Create 20 notebooks across followed users
    - GET /api/v1/feed?limit=10
    - Verify 10 notebooks returned
    - Verify cursor present
    - GET /api/v1/feed?cursor={cursor}
    - Verify next 10 notebooks (no duplicates)

    Test: Trending fallback
    - Create user A with no follows
    - Create trending notebooks (high engagement)
    - GET /api/v1/feed (as user A)
    - Verify feed returns trending notebooks (fallback)
    - Verify notebooks sorted by trending score

    Test: Feed excludes unpublished
    - Create published and draft notebooks
    - GET /api/v1/feed
    - Verify only published notebooks in feed
    - Verify draft notebooks not in feed

    Test: Feed cache invalidation
    - Create notebook and publish
    - GET /api/v1/feed (cached)
    - Update notebook (trigger cache invalidation)
    - GET /api/v1/feed (cache miss)
    - Verify updated notebook in feed

    Test: Trending algorithm
    - Create notebooks with varying engagement:
      - Notebook A: 20 likes, 10 comments (old)
      - Notebook B: 15 likes, 5 comments (new)
    - Call trending recalculation (simulate Celery beat)
    - GET /api/v1/feed/trending
    - Verify notebooks ranked by time-decayed score
    - Verify newer content with similar engagement ranks higher

    Test: Engagement tracking
    - Create notebook
    - POST /api/v1/engagement with notebook_id and type='view'
    - Verify Redis increment called
    - Create multiple engagements
    - Verify score updated in cache

    Use real PostgreSQL for queries
    Use real Redis for caching
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/integration/test_feed_flow.py -v -m integration</automated>
  </verify>
  <done>Feed integration tests verify personalized feed, trending fallback, pagination, and caching</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. All integration tests pass:
   - pytest tests/integration/ -v -m integration shows all tests passing
   - Each test uses real PostgreSQL and Redis

2. Test coverage of 6 flows:
   - test_auth_flow.py: OAuth, user creation, session
   - test_notebook_lifecycle.py: create, edit, compile, publish, view
   - test_forking_flow.py: fork, lineage, independent edits, delete protection
   - test_social_flow.py: follow, like, comment, feed updates
   - test_search_flow.py: index, search, filter
   - test_feed_flow.py: personalized feed, trending, pagination

3. Database isolation:
   - Tests don't share state (rollback works)
   - Running tests in parallel doesn't cause failures

4. Mock strategy correct:
   - External services mocked (OAuth, S3, Meilisearch)
   - Internal services not mocked (PostgreSQL, Redis)

5. Smoke tests catch mock drift:
   - At least 2-3 tests use real PostgreSQL/Redis without mocks
</verification>

<success_criteria>
1. 6 integration test files created covering all major user flows
2. All integration tests pass with real PostgreSQL and Redis
3. Multi-endpoint workflows verified end-to-end
4. Database rollback isolation confirmed
5. Mock drift detected via smoke tests (if any)
6. CI/CD workflow includes integration test job
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-quality/05-03-SUMMARY.md` with:
- Summary of integration tests created
- List of user flows covered
- Database and Redis usage patterns
- Any mock drift detected
- Test execution times
</output>
