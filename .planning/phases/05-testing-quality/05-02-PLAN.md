---
phase: 05-testing-quality
plan: 02
type: execute
wave: 2
depends_on: [05-00]
files_modified: ["frontend/tests/**/*.test.tsx"]
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "All UI components have component tests with 80%+ coverage"
    - "Component tests use @testing-library/react for user-centric queries"
    - "API calls are mocked using MSW or fetch interception"
    - "Zustand stores are tested directly (no mocking)"
    - "Tests cover all states: loading, success, error, empty, disabled"
  artifacts:
    - path: "frontend/tests/components/auth/"
      provides: "Auth component tests"
      contains: ["OAuthButton.test.tsx", "ProtectedRoute.test.tsx"]
    - path: "frontend/tests/components/notebook/"
      provides: "Notebook component tests"
      contains: ["NotebookEditor.test.tsx", "NotebookCard.test.tsx", "NotebookViewer.test.tsx", "CompilationDialog.test.tsx", "PublishDialog.test.tsx"]
    - path: "frontend/tests/components/social/"
      provides: "Social component tests"
      contains: ["LikeButton.test.tsx", "CommentForm.test.tsx", "CommentList.test.tsx", "FollowButton.test.tsx"]
    - path: "frontend/tests/components/search/"
      provides: "Search component tests"
      contains: ["SearchBar.test.tsx", "FilterTabs.test.tsx", "SearchResults.test.tsx"]
    - path: "frontend/tests/components/feed/"
      provides: "Feed component tests"
      contains: ["FeedList.test.tsx", "FeedCard.test.tsx", "TrendingList.test.tsx"]
    - path: "frontend/tests/stores/"
      provides: "Store tests"
      contains: ["auth-store.test.ts", "notebook-store.test.ts", "social-store.test.ts", "feed-store.test.ts"]
  key_links:
    - from: "tests/setup.ts"
      to: "all component tests"
      via: "global mocks and test utilities"
      pattern: "import { render, screen } from '@testing-library/react'"
    - from: "component files"
      to: "test files"
      via: "co-located test pattern"
      pattern: "__tests__/ or tests/components/"
---

<objective>
Create comprehensive component tests for all frontend UI components and stores

Purpose: Achieve 80%+ line coverage for frontend code by testing all React components and Zustand stores. Tests use @testing-library/react for user-centric queries per D-07 and direct store testing per D-09.

Output: 30+ test files covering all components and stores, with 80%+ line coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-quality/05-CONTEXT.md
@.planning/phases/05-testing-quality/05-RESEARCH.md
@frontend/vitest.config.ts
@frontend/tests/setup.ts
@frontend/package.json

# Locked Decisions from CONTEXT.md
- D-06: Hybrid component testing (shallow for props, full DOM for interactions)
- D-07: Use @testing-library/react for user-centric queries
- D-08: HTTP-level mocking for API calls
- D-09: Direct store testing for Zustand
- D-10: Comprehensive interaction testing (all states)

# Frontend Components (from frontend/components/)
## Auth (frontend/components/auth/)
- OAuthButton (Google, Facebook login)
- ProtectedRoute (auth guard)

## Notebook (frontend/components/notebook/)
- NotebookEditor (Monaco + Pyodide)
- NotebookCard (feed card)
- NotebookViewer (read-only view)
- CellEditor (code/markdown cell)
- CompilationDialog (compile status)
- PublishDialog (publish workflow)
- DatasetsDialog (dataset selection)

## Social (frontend/components/social/)
- LikeButton (toggle like)
- CommentForm (create comment)
- CommentList (display comments)
- CommentItem (single comment with replies)
- FollowButton (toggle follow)

## Search (frontend/components/search/)
- SearchBar (search input)
- FilterTabs (original/fork/all filter)
- SearchResults (search results list)

## Feed (frontend/components/feed/)
- FeedList (infinite scroll feed)
- TrendingList (trending notebooks)
- EngagementMetrics (views, likes, comments)

## UI (frontend/components/ui/)
- (shadcn/ui components - skip testing, well-tested library)

# Zustand Stores (from frontend/stores/)
- auth-store.ts (auth state, login/logout)
- notebook-store.ts (editor state, cells)
- compilation-store.ts (compilation status)
- feed-store.ts (feed state, pagination)
- social-store.ts (likes, comments, follows)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for auth components</name>
  <files>frontend/tests/components/auth/OAuthButton.test.tsx, frontend/tests/components/auth/ProtectedRoute.test.tsx</files>
  <action>
    Create frontend/tests/components/auth/OAuthButton.test.tsx:

    1. Test Google OAuth button renders:
       - Verify button with "Continue with Google" text
       - Verify Google icon displayed
       - Verify href includes /api/v1/auth/google

    2. Test Facebook OAuth button renders:
       - Verify button with "Continue with Facebook" text
       - Verify Facebook icon displayed
       - Verify href includes /api/v1/auth/facebook

    3. Test button disabled state:
       - Pass disabled prop
       - Verify button is disabled
       - Verify loading spinner shown

    4. Test click handler (if using client-side flow):
       - Mock window.location.assign
       - Verify OAuth URL called on click

    Create frontend/tests/components/auth/ProtectedRoute.test.tsx:

    1. Test redirect when not authenticated:
       - Mock auth store: isAuthenticated = false
       - Render protected component
       - Verify redirect to /login

    2. Test render when authenticated:
       - Mock auth store: isAuthenticated = true
       - Render protected component
       - Verify child component renders

    3. Test loading state:
       - Mock auth store: isLoading = true
       - Verify loading spinner shown

    Use @testing-library/react: render, screen
    Use vi.mock for store mocking
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- OAuthButton.test.tsx --run</automated>
  </verify>
  <done>Auth components have tests covering OAuth buttons and protected route with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for notebook editor components</name>
  <files>frontend/tests/components/notebook/NotebookEditor.test.tsx, frontend/tests/components/notebook/CellEditor.test.tsx</files>
  <action>
    Create frontend/tests/components/notebook/NotebookEditor.test.tsx:

    1. Test editor renders with existing cells:
       - Mock notebook store with cells
       - Verify all cells rendered
       - Verify code cells have Monaco editor
       - Verify markdown cells rendered

    2. Test add code cell:
       - Mock store actions
       - Click "Add Code Cell" button
       - Verify addCodeCell action called
       - Verify new cell rendered

    3. Test add markdown cell:
       - Click "Add Markdown Cell" button
       - Verify addMarkdownCell action called

    4. Test delete cell:
       - Mock cell with delete button
       - Click delete
       - Verify deleteCell action called

    5. Test update cell content:
       - Type in code cell
       - Verify updateCellContent action called with new content

    6. Test save notebook:
       - Click save button
       - Verify saveNotebook action called
       - Verify success toast shown

    7. Test Pyodide initialization:
       - Mock Pyodide loading
       - Verify loading state shown
       - Verify editor enabled after load

    Create frontend/tests/components/notebook/CellEditor.test.tsx:

    1. Test code cell with Monaco:
       - Render code cell
       - Verify Monaco editor component rendered
       - Verify language set to python

    2. Test markdown cell with preview:
       - Render markdown cell
       - Verify markdown preview rendered
       - Verify edit mode toggles preview

    3. Test cell execution:
       - Click run button
       - Verify executeCell action called

    Use userEvent for realistic user interactions
    Use vi.fn() for action mocks
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- NotebookEditor.test.tsx --run</automated>
  </verify>
  <done>Notebook editor components have tests covering cell management and editor interactions with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create tests for notebook card and viewer components</name>
  <files>frontend/tests/components/notebook/NotebookCard.test.tsx, frontend/tests/components/notebook/NotebookViewer.test.tsx</files>
  <action>
    Create frontend/tests/components/notebook/NotebookCard.test.tsx:

    1. Test card renders with notebook data:
       - Render with mock notebook (title, author, stats)
       - Verify title displayed
       - Verify author username displayed
       - Verify like and comment counts displayed

    2. Test card click navigation:
       - Mock router.push
       - Click card
       - Verify navigation to /notebooks/{id}

    3. Test like button:
       - Click like button
       - Verify toggleLike action called
       - Verify optimistic update (icon changes)

    4. Test fork indicator:
       - Render fork notebook
       - Verify "Forked from" link shown
       - Verify parent_id tracked

    5. Test published badge:
       - Render published notebook
       - Verify "Published" badge shown
       - Render draft notebook
       - Verify no badge

    Create frontend/tests/components/notebook/NotebookViewer.test.tsx:

    1. Test viewer renders notebook:
       - Render with mock notebook
       - Verify title displayed
       - Verify all cells rendered
       - Verify outputs displayed (not editable)

    2. Test code cell execution disabled:
       - Verify no "Run" button
       - Verify code cells are read-only

    3. Test fork button:
       - Click fork button
       - Verify navigation to editor with forked notebook

    4. Test empty notebook:
       - Render notebook with no cells
       - Verify empty state message shown

    5. Test loading state:
       - Render with loading=true
       - Verify skeleton shown

    6. Test error state:
       - Render with error
       - Verify error message shown
       - Verify retry button shown
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- NotebookCard.test.tsx NotebookViewer.test.tsx --run</automated>
  </verify>
  <done>Notebook card and viewer components have tests covering display and interactions with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 4: Create tests for compilation and publish dialogs</name>
  <files>frontend/tests/components/notebook/CompilationDialog.test.tsx, frontend/tests/components/notebook/PublishDialog.test.tsx</files>
  <action>
    Create frontend/tests/components/notebook/CompilationDialog.test.tsx:

    1. Test dialog opens and closes:
       - Render dialog with open=true
       - Verify dialog content visible
       - Click close button
       - Verify dialog closed

    2. Test compilation states:
       - Render with status='idle'
       - Verify "Compile" button enabled
       - Render with status='compiling'
       - Verify loading spinner, button disabled
       - Render with status='success'
       - Verify success message
       - Render with status='error'
       - Verify error message

    3. Test start compilation:
       - Click compile button
       - Verify startCompilation action called
       - Verify status changes to compiling

    4. Test dataset selection:
       - Mock available datasets
       - Verify dataset dropdown shown
       - Select dataset
       - Verify dataset_id passed to action

    5. Test poll compilation status:
       - Mock polling interval
       - Verify status polled every 2 seconds
       - Verify polling stops on success/error

    Create frontend/tests/components/notebook/PublishDialog.test.tsx:

    1. Test dialog opens with precompiled notebook:
       - Render with compiled output
       - Verify output preview shown
       - Verify "Publish" button enabled

    2. Test dialog opens without compilation:
       - Render without compilation
       - Verify "Compile first" message
       - Verify "Publish" button disabled

    3. Test publish workflow:
       - Click publish button
       - Verify publishNotebook action called
       - Verify success toast shown
       - Verify dialog closes

    4. Test cancel:
       - Click cancel button
       - Verify dialog closes without publishing

    5. Test output URL preview:
       - Render with S3 output URL
       - Verify link to output shown
       - Click link
       - Verify opens in new tab

    Test all states per D-10: loading, success, error, empty, disabled
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- CompilationDialog.test.tsx PublishDialog.test.tsx --run</automated>
  </verify>
  <done>Compilation and publish dialogs have tests covering all states and workflows with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 5: Create tests for social components</name>
  <files>frontend/tests/components/social/LikeButton.test.tsx, frontend/tests/components/social/CommentForm.test.tsx, frontend/tests/components/social/CommentList.test.tsx, frontend/tests/components/social/FollowButton.test.tsx</files>
  <action>
    Create frontend/tests/components/social/LikeButton.test.tsx:

    1. Test like button when not liked:
       - Render with isLiked=false
       - Verify outline heart icon
       - Click button
       - Verify toggleLike action called

    2. Test like button when liked:
       - Render with isLiked=true
       - Verify filled heart icon
       - Click button
       - Verify unlike action called

    3. Test optimistic update:
       - Click like button
       - Verify icon changes immediately (before API response)
       - Verify count increments

    4. Test disabled state:
       - Render with disabled=true
       - Verify button disabled
       - Click does not trigger action

    5. Test loading state:
       - Render with loading=true
       - Verify spinner shown

    Create frontend/tests/components/social/CommentForm.test.tsx:

    1. Test form renders:
       - Verify textarea displayed
       - Verify "Post" button displayed

    2. Test submit comment:
       - Type comment in textarea
       - Click post button
       - Verify createComment action called with content

    3. Test validation:
       - Submit empty comment
       - Verify validation error shown
       - Verify action not called

    4. Test character limit:
       - Type 1000+ characters
       - Verify character counter shown
       - Verify submission blocked over limit

    5. Test reply mode:
       - Render with replyingTo comment
       - Verify "Replying to @username" shown
       - Verify parent_id passed to action

    Create frontend/tests/components/social/CommentList.test.tsx:

    1. Test list renders comments:
       - Render with mock comments
       - Verify all comments displayed
       - Verify author names shown
       - Verify timestamps shown

    2. Test nested replies:
       - Render with parent comment and replies
       - Verify replies indented
       - Verify reply count shown

    3. Test empty state:
       - Render with empty comments array
       - Verify "No comments yet" message

    4. Test load more:
       - Render with hasMore=true
       - Verify "Load more" button shown
       - Click button
       - Verify loadMoreComments action called

    Create frontend/tests/components/social/FollowButton.test.tsx:

    1. Test follow button when not following:
       - Render with isFollowing=false
       - Verify "Follow" text
       - Click button
       - Verify toggleFollow action called

    2. Test unfollow button when following:
       - Render with isFollowing=true
       - Verify "Following" text
       - Click button
       - Verify unfollow action called

    3. Test self profile (no button):
       - Render with isSelf=true
       - Verify button not rendered

    Use @testing-library/react queries: getByRole, getByText
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- tests/components/social/ --run</automated>
  </verify>
  <done>Social components have tests covering likes, comments, and follows with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 6: Create tests for search components</name>
  <files>frontend/tests/components/search/SearchBar.test.tsx, frontend/tests/components/search/FilterTabs.test.tsx, frontend/tests/components/search/SearchResults.test.tsx</files>
  <action>
    Create frontend/tests/components/search/SearchBar.test.tsx:

    1. Test search input:
       - Verify input field rendered
       - Type search query
       - Verify onSearchChange called with query

    2. Test search on enter:
       - Type query
       - Press Enter
       - Verify search action called

    3. Test search button:
       - Click search button
       - Verify search action called

    4. Test clear button:
       - Type query
       - Click clear button
       - Verify input cleared
       - Verify search action called with empty string

    5. Test debounce:
       - Mock setTimeout
       - Type query rapidly
       - Verify search called after debounce delay

    Create frontend/tests/components/search/FilterTabs.test.tsx:

    1. Test filter tabs render:
       - Verify "All", "Original", "Forks" tabs shown
       - Verify active tab highlighted

    2. Test filter selection:
       - Click "Original" tab
       - Verify onFilterChange called with 'original'
       - Click "Forks" tab
       - Verify onFilterChange called with 'fork'

    3. Test active state:
       - Render with filter='forks'
       - Verify "Forks" tab active

    Create frontend/tests/components/search/SearchResults.test.tsx:

    1. Test results render:
       - Render with mock notebooks
       - Verify all notebooks shown as cards
       - Verify result count displayed

    2. Test empty state:
       - Render with empty results
       - Verify "No results found" message

    3. Test loading state:
       - Render with loading=true
       - Verify skeleton loaders shown

    4. Test error state:
       - Render with error
       - Verify error message shown

    5. Test highlight matches:
       - Render with search query
       - Verify matching text highlighted in results

    6. Test filter badge:
       - Render with filter='forks'
       - Verify "Forks only" badge shown
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- tests/components/search/ --run</automated>
  </verify>
  <done>Search components have tests covering search input, filters, and results with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 7: Create tests for feed components</name>
  <files>frontend/tests/components/feed/FeedList.test.tsx, frontend/tests/components/feed/TrendingList.test.tsx, frontend/tests/components/feed/EngagementMetrics.test.tsx</files>
  <action>
    Create frontend/tests/components/feed/FeedList.test.tsx:

    1. Test feed renders notebooks:
       - Render with mock notebooks
       - Verify all notebook cards rendered
       - Verify infinite scroll enabled

    2. Test load more on scroll:
       - Mock intersection observer
       - Scroll to bottom
       - Verify loadMore action called

    3. Test empty feed:
       - Render with empty notebooks
       - Verify "No notebooks yet" message
       - Verify "Create your first notebook" CTA shown

    4. Test loading more indicator:
       - Render with isLoadingMore=true
       - Verify spinner at bottom

    5. Test end of feed:
       - Render with hasMore=false
       - Verify "You're all caught up" message

    Create frontend/tests/components/feed/TrendingList.test.tsx:

    1. Test trending renders:
       - Render with mock trending notebooks
       - Verify notebooks shown in ranked order
       - Verify rank badges (#1, #2, #3) shown

    2. Test refresh:
       - Click refresh button
       - Verify refreshTrending action called

    3. Test time decay indicator:
       - Render with timestamps
       - Verify "Trending now" for fresh content
       - Verify "Trending past 24h" for older content

    Create frontend/tests/components/feed/EngagementMetrics.test.tsx:

    1. Test metrics display:
       - Render with views, likes, comments
       - Verify all metrics shown
       - Verify icons displayed

    2. Test format large numbers:
       - Render with 1500 views
       - Verify "1.5K" format
       - Render with 1000000 views
       - Verify "1M" format

    3. Test view tracking:
       - Render with trackView=true
       - Verify incrementView action called on mount

    Test all states per D-10
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- tests/components/feed/ --run</automated>
  </verify>
  <done>Feed components have tests covering feed rendering, infinite scroll, and trending with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 8: Create tests for Zustand stores</name>
  <files>frontend/tests/stores/auth-store.test.ts, frontend/tests/stores/notebook-store.test.ts, frontend/tests/stores/social-store.test.ts, frontend/tests/stores/feed-store.test.ts</files>
  <action>
    Create frontend/tests/stores/auth-store.test.ts:

    1. Test initial state:
       - Verify isAuthenticated = false
       - Verify user = null
       - Verify isLoading = false

    2. Test login action:
       - Call login(user)
       - Verify user set
       - Verify isAuthenticated = true
       - Verify token saved to cookie

    3. Test logout action:
       - Set authenticated state
       - Call logout()
       - Verify user = null
       - Verify isAuthenticated = false
       - Verify cookies cleared

    4. Test update user action:
       - Set user
       - Call updateUser({ bio: 'new bio' })
       - Verify user.bio updated

    Create frontend/tests/stores/notebook-store.test.ts:

    1. Test initial state:
       - Verify cells = []
       - Verify notebookId = null

    2. Test load notebook:
       - Call loadNotebook(notebook)
       - Verify cells set
       - Verify notebookId set

    3. Test add code cell:
       - Call addCodeCell()
       - Verify new cell added with type='code'
       - Verify order_index correct

    4. Test add markdown cell:
       - Call addMarkdownCell()
       - Verify new cell added with type='markdown'

    5. Test update cell:
       - Add cell
       - Call updateCell(id, content)
       - Verify cell.content updated

    6. Test delete cell:
       - Add cell
       - Call deleteCell(id)
       - Verify cell removed

    7. Test persist to localStorage:
       - Modify state
       - Verify localStorage updated

    Create frontend/tests/stores/social-store.test.ts:

    1. Test initial state:
       - Verify likedNotebooks = Set()
       - Verify comments = []

    2. Test toggle like:
       - Call toggleLike(notebookId)
       - Verify notebookId added to set
       - Call toggleLike(notebookId) again
       - Verify notebookId removed

    3. Test set comments:
       - Call setComments(comments)
       - Verify comments set
       - Verify nested replies structured correctly

    4. Test add comment (optimistic):
       - Call addComment(tempComment)
       - Verify comment added to state
       - Verify isOptimistic = true

    5. Test update follow:
       - Call updateFollow(userId, true)
       - Verify following.add(userId)

    Create frontend/tests/stores/feed-store.test.ts:

    1. Test initial state:
       - Verify notebooks = []
       - Verify cursor = null
       - Verify hasMore = true

    2. Test load notebooks:
       - Call loadNotebooks({ notebooks, cursor, hasMore })
       - Verify state updated

    3. Test append notebooks (infinite scroll):
       - Set existing notebooks
       - Call loadNotebooks with new notebooks
       - Verify notebooks appended

    4. Test reset feed:
       - Call resetFeed()
       - Verify notebooks = []
       - Verify cursor = null

    Direct store testing per D-09 - no mocking, import and test directly
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm test -- tests/stores/ --run</automated>
  </verify>
  <done>All Zustand stores have tests covering state management and actions with 80%+ coverage</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. All component tests pass:
   - npm test -- --run shows all tests passing
   - Coverage report shows 80%+ line coverage for components

2. All store tests pass:
   - npm test -- tests/stores/ --run shows all tests passing
   - Coverage report shows 80%+ line coverage for stores

3. Total test count:
   - npm test -- --run --reporter=verbose shows 50+ tests

4. Coverage thresholds met:
   - npm run test:coverage shows 80%+ line coverage
   - Branch coverage 70%+

5. All component states tested:
   - Each component has tests for: loading, success, error, empty, disabled states

6. User-centric queries used:
   - No getByTestId queries
   - All queries use getByRole, getByText, getByLabelText

7. API calls properly mocked:
   - All fetch calls intercepted
   - No actual API calls during tests
</verification>

<success_criteria>
1. 30+ test files created covering all components and stores
2. 80+ component and store tests written and passing
3. 80%+ line coverage achieved for frontend code
4. 70%+ branch coverage achieved for frontend code
5. All UI states tested (loading, success, error, empty, disabled)
6. User-centric queries used throughout (no testid)
7. Direct store testing (no mocking of stores)
8. API calls mocked via MSW or fetch interception
9. CI/CD workflow (test-frontend.yml) passes when run
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-quality/05-02-SUMMARY.md` with:
- Summary of frontend component tests created
- Coverage report by component category
- List of components tested
- Any uncovered code paths and rationale
- Total test count
</output>
