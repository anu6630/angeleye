---
phase: 05-testing-quality
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "All test dependencies are installed (pytest, vitest, playwright, k6)"
    - "Test directory structure exists for unit, integration, and E2E tests"
    - "Coverage tools are configured (coverage.py, vitest coverage)"
    - "Code quality tools are configured (black, ruff, mypy, eslint)"
    - "GitHub Actions workflows exist for all test types"
    - "Playwright is initialized with Chromium browser"
  artifacts:
    - path: "backend/tests/"
      provides: "Test directory structure"
      contains: ["unit/", "integration/", "test_factories.py"]
    - path: "frontend/tests/"
      provides: "Frontend test directory"
      contains: ["components/", "e2e/", "setup.ts"]
    - path: "backend/requirements.txt"
      provides: "Test dependencies"
      contains: ["pytest", "pytest-cov", "pytest-mock", "black", "ruff", "mypy", "bandit"]
    - path: "frontend/package.json"
      provides: "Frontend test dependencies"
      contains: ["@playwright/test", "@vitest/coverage-v8"]
    - path: ".github/workflows/"
      provides: "CI/CD workflows"
      contains: ["test-backend.yml", "test-frontend.yml", "test-e2e.yml", "code-quality.yml"]
    - path: "frontend/playwright.config.ts"
      provides: "Playwright configuration"
      contains: "Chromium browser config"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration"
      contains: "Coverage settings"
  key_links:
    - from: "pytest.ini"
      to: "backend/tests/"
      via: "testpaths configuration"
      pattern: "testpaths = tests"
    - from: "vitest.config.ts"
      to: "frontend/tests/"
      via: "include pattern"
      pattern: "'**/*.test.ts'"
---

<objective>
Establish test infrastructure and CI/CD foundation for comprehensive testing across the entire stack

Purpose: Create the foundation for all subsequent test waves by installing dependencies, configuring tools, and setting up CI/CD pipelines. This wave ensures all testing frameworks are properly configured before writing actual tests.

Output: Working test infrastructure with all dependencies installed, directories created, and CI/CD workflows ready to run tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-quality/05-CONTEXT.md
@.planning/phases/05-testing-quality/05-RESEARCH.md
@.planning/ROADMAP.md
@backend/pytest.ini
@backend/tests/conftest.py
@frontend/package.json

# Key Decisions from CONTEXT.md (Locked - D-01 through D-18)
- D-01: Target 80%+ line coverage and 70%+ branch coverage
- D-02: Aggressive mocking for external services (OAuth, S3, Meilisearch, Redis)
- D-03: Factory pattern for test data
- D-04: Database isolation via rollback transactions
- D-14: GitHub Actions only for CI/CD
- D-15: Full parallel test execution
- D-16: Strict failure policy for flaky tests
- D-18: Comprehensive code quality gates
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install backend test dependencies and configure coverage</name>
  <files>backend/requirements.txt, backend/pytest.ini, backend/.coveragerc, backend/pyproject.toml</files>
  <action>
    Install missing test dependencies:
    - Add to requirements.txt: pytest-cov==5.0.0, pytest-mock==3.14.0, pytest-xdist==3.6.1
    - Add code quality tools: black==24.0.0, ruff==0.9.0, mypy==1.13.0, bandit==1.8.0

    Create backend/.coveragerc with:
    - Omit tests/, migrations/, __init__.py, conftest.py
    - Set branch coverage threshold to 70
    - Set line coverage threshold to 80

    Create backend/pyproject.toml with:
    - [tool.black] config (line-length=100, target-version=py39)
    - [tool.ruff] config (line-length=100, select=E,F,W)
    - [tool.mypy] config (python_version=3.9, warn_return_any=true)

    Update pytest.ini addopts:
    - Add --cov=app --cov-report=term-missing --cov-report=html
    - Add --cov-fail-under=70 (branch threshold)

    Run: pip install -r requirements.txt to verify all dependencies install correctly.
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pip list | grep -E "pytest|black|ruff|mypy|bandit"</automated>
  </verify>
  <done>All backend test dependencies installed, coverage configured with 70% branch threshold, code quality tools configured</done>
</task>

<task type="auto">
  <name>Task 2: Create backend test directory structure and factory functions</name>
  <files>backend/tests/unit/, backend/tests/integration/, backend/tests/test_factories.py</files>
  <action>
    Create test directory structure:
    - backend/tests/unit/ - for fast unit tests with mocks
    - backend/tests/integration/ - for multi-endpoint tests with real DB
    - backend/tests/e2e/ - for full workflow tests (placeholder for Wave 4)

    Create backend/tests/test_factories.py with factory functions per D-03:
    - create_user(db_session, **kwargs) - creates User with oauth_provider, oauth_id, username, email
    - create_notebook(db_session, user_id, **kwargs) - creates Notebook with cells
    - create_notebook_cell(db_session, notebook_id, **kwargs) - creates NotebookCell
    - create_like(db_session, user_id, notebook_id) - creates Like
    - create_comment(db_session, user_id, notebook_id, parent_id=None) - creates Comment
    - create_fork(db_session, notebook_id, user_id, **kwargs) - creates Notebook fork with parent_id/root_id
    - create_follow(db_session, follower_id, following_id) - creates Follow

    Each factory should:
    - Accept optional kwargs for field overrides
    - Commit to database and refresh
    - Return the model instance
    - Handle foreign key relationships automatically

    Reference existing test_user and test_notebook fixtures in conftest.py for patterns.
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && python -c "from tests.test_factories import create_user, create_notebook; print('Factories imported successfully')"</automated>
  </verify>
  <done>Test directories created, factory functions for all major models implemented</done>
</task>

<task type="auto">
  <name>Task 3: Install frontend test dependencies and configure Vitest</name>
  <files>frontend/package.json, frontend/vitest.config.ts</files>
  <action>
    Install frontend test dependencies:
    - npm install -D @playwright/test @vitest/coverage-v8
    - npx playwright install chromium (skip chrome/edge per D-13)

    Create frontend/vitest.config.ts:
    - Set test.match: ['**/*.test.ts', '**/*.test.tsx']
    - Configure coverage with @vitest/coverage-v8
    - Set coverage.threshold: { lines: 80, branches: 70, functions: 80 }
    - Set globals: true for describe/it/expect global availability
    - Set environment: 'jsdom' for component testing
    - Include setup file: './tests/setup.ts'

    Create frontend/tests/setup.ts:
    - Import @testing-library/jest-dom for custom matchers
    - Configure global mocks (window.matchMedia, IntersectionObserver)
    - Mock MSW (Mock Service Worker) for API interception in Wave 2

    Update package.json scripts:
    - "test": "vitest"
    - "test:ui": "vitest --ui"
    - "test:coverage": "vitest run --coverage"
    - "test:e2e": "playwright test"
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npm list @playwright/test @vitest/coverage-v8 2>&1 | grep -E "@playwright|@vitest"</automated>
  </verify>
  <done>Frontend test dependencies installed, Vitest configured with coverage thresholds, test setup file created</done>
</task>

<task type="auto">
  <name>Task 4: Create frontend test directory structure and Playwright config</name>
  <files>frontend/tests/components/, frontend/tests/e2e/, frontend/playwright.config.ts</files>
  <action>
    Create frontend test directory structure:
    - frontend/tests/components/auth/ - OAuthButton, ProtectedRoute tests
    - frontend/tests/components/notebook/ - NotebookEditor, NotebookCard, NotebookViewer tests
    - frontend/tests/components/social/ - LikeButton, CommentForm, FollowButton tests
    - frontend/tests/components/search/ - SearchBar, FilterTabs tests
    - frontend/tests/components/feed/ - FeedList, NotebookCard tests
    - frontend/tests/e2e/ - Playwright E2E tests
    - frontend/tests/mocks/ - API mocks for MSW

    Create frontend/playwright.config.ts:
    - Set projects: [{ name: 'chromium', use: { browserName: 'chromium' } }]
    - Set testDir: './tests/e2e'
    - Set fullyParallel: true
    - Set retries: 0 (strict failure policy per D-16)
    - Set workers: process.platform === 'darwin' ? 4 : 2 (parallel execution per D-15)
    - Set use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry' }
    - Set webServer: { command: 'npm run dev', port: 3000, timeout: 120000 }
    - Disable video and screenshot for faster CI (add only on failure)

    Create placeholder test files to verify structure:
    - frontend/tests/components/auth/example.test.tsx (empty describe)
    - frontend/tests/e2e/example.spec.ts (empty test)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/frontend && npx playwright test --list 2>&1 | head -5</automated>
  </verify>
  <done>Frontend test directories created, Playwright configured with Chromium-only and test server setup</done>
</task>

<task type="auto">
  <name>Task 5: Create GitHub Actions workflows for backend testing</name>
  <files>.github/workflows/test-backend.yml, .github/workflows/code-quality.yml</files>
  <action>
    Create .github/workflows/test-backend.yml:
    - Trigger on: push, pull_request to main
    - Job: test-backend with ubuntu-latest
    - Steps:
      1. Checkout code
      2. Set up Python 3.9
      3. Cache pip dependencies (actions/cache with key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }})
      4. Install dependencies: pip install -r backend/requirements.txt
      5. Run unit tests in parallel: pytest tests/unit -n auto -v --cov=app --cov-report=xml
      6. Run integration tests: pytest tests/integration -v -m integration
      7. Upload coverage to Codecov (optional, or just report in summary)
    - Use matrix strategy if needed for different Python versions (not needed for v1)

    Create .github/workflows/code-quality.yml:
    - Trigger on: push, pull_request
    - Jobs:
      1. backend-lint: Run black --check backend/, ruff check backend/
      2. backend-typecheck: Run mypy backend/app
      3. backend-security: Run bandit -r backend/app/
      4. frontend-lint: Run eslint frontend/ (use existing next/core-web-vitals config)
      5. frontend-typecheck: Run tsc --noEmit in frontend/

    All jobs should run in parallel for fast feedback (D-15).
  </action>
  <verify>
    <automated>ls -la /Volumes/SSDX/codes/time/.github/workflows/ | grep -E "test-backend|code-quality"</automated>
  </verify>
  <done>GitHub Actions workflows created for backend tests and code quality gates</done>
</task>

<task type="auto">
  <name>Task 6: Create GitHub Actions workflows for frontend and E2E testing</name>
  <files>.github/workflows/test-frontend.yml, .github/workflows/test-e2e.yml</files>
  <action>
    Create .github/workflows/test-frontend.yml:
    - Trigger on: push, pull_request to main
    - Job: test-frontend with ubuntu-latest
    - Steps:
      1. Checkout code
      2. Set up Node.js 25
      3. Cache node_modules (actions/cache with key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }})
      4. Install dependencies: npm ci in frontend/
      5. Run unit tests: npm run test:coverage
      6. Upload coverage report

    Create .github/workflows/test-e2e.yml:
    - Trigger on: push, pull_request to main
    - Job: test-e2e with ubuntu-latest
    - Steps:
      1. Checkout code
      2. Set up Node.js 25
      3. Install dependencies: npm ci in frontend/
      4. Install Playwright browsers: npx playwright install --with-deps chromium
      5. Install backend dependencies and start services (use docker-compose for PostgreSQL/Redis)
      6. Start backend server (background job)
      7. Run E2E tests: npm run test:e2e
      8. Upload test results and artifacts on failure
    - Set timeout-minutes: 20 for E2E tests

    Note: E2E tests will be implemented in Wave 4, but workflow should be ready.
  </action>
  <verify>
    <automated>ls -la /Volumes/SSDX/codes/time/.github/workflows/ | grep -E "test-frontend|test-e2e"</automated>
  </verify>
  <done>GitHub Actions workflows created for frontend tests and E2E tests</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Backend dependencies installed:
   - pytest --version shows 9.0.2
   - pytest-cov --version shows 5.0.0
   - black --version shows 24.0.0
   - ruff --version shows 0.9.0

2. Frontend dependencies installed:
   - npm list @playwright/test shows installed
   - npm list @vitest/coverage-v8 shows installed
   - npx playwright --version shows installed

3. Test directories exist:
   - backend/tests/unit/ exists
   - backend/tests/integration/ exists
   - backend/tests/test_factories.py exists
   - frontend/tests/components/ exists with subdirectories
   - frontend/tests/e2e/ exists

4. Configuration files valid:
   - pytest.ini has --cov settings
   - vitest.config.ts exists and has coverage config
   - playwright.config.ts exists and has Chromium config
   - .coveragerc exists with omit patterns

5. GitHub Actions workflows exist:
   - .github/workflows/test-backend.yml
   - .github/workflows/test-frontend.yml
   - .github/workflows/test-e2e.yml
   - .github/workflows/code-quality.yml

6. Factory functions importable:
   - from tests.test_factories import create_user works
   - from tests.test_factories import create_notebook works
   - from tests.test_factories import create_fork works
</verification>

<success_criteria>
1. All test dependencies installed and verified via pip list and npm list
2. Test directory structure created for backend (unit, integration) and frontend (components, e2e)
3. Coverage tools configured with 70% branch threshold and 80% line threshold
4. Code quality tools configured (black, ruff, mypy, bandit, eslint)
5. GitHub Actions workflows created for all test types (backend, frontend, E2E, quality)
6. Playwright initialized with Chromium browser only
7. Factory functions created for all major models (User, Notebook, Like, Comment, Fork, Follow)
8. Test configurations use aggressive mocking strategy per D-02
9. Test isolation uses rollback transactions per D-4 (in conftest.py, already exists)
10. CI/CD configured for parallel execution per D-15
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-quality/05-00-SUMMARY.md` with:
- Summary of test infrastructure created
- List of all dependencies installed with versions
- Test directory structure diagram
- GitHub Actions workflow summary
- Factory functions created
- Any issues encountered during setup
</output>
