---
phase: 05-testing-quality
plan: 01
type: execute
wave: 1
depends_on: [05-00]
files_modified: ["backend/tests/unit/*.py"]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "All service classes have unit tests with 80%+ line coverage"
    - "All API endpoints have unit tests covering success and error cases"
    - "External services are mocked (OAuth, S3, Meilisearch, Redis)"
    - "Database operations use rollback transaction isolation"
    - "Tests verify business logic, not implementation details"
  artifacts:
    - path: "backend/tests/unit/test_auth_service.py"
      provides: "AuthService unit tests"
      contains: "Tests for OAuth flow, user creation, token validation"
    - path: "backend/tests/unit/test_notebook_service.py"
      provides: "NotebookService unit tests"
      contains: "Tests for CRUD operations, cell management"
    - path: "backend/tests/unit/test_fork_service.py"
      provides: "ForkService unit tests"
      contains: "Tests for forking logic, lineage tracking, delete protection"
    - path: "backend/tests/unit/test_social_service.py"
      provides: "LikeService and CommentService unit tests"
      contains: "Tests for likes, comments, threaded replies"
    - path: "backend/tests/unit/test_search_service.py"
      provides: "SearchService unit tests"
      contains: "Tests for search indexing, querying, filtering"
    - path: "backend/tests/unit/test_feed_service.py"
      provides: "FeedService and TrendingService unit tests"
      contains: "Tests for feed generation, trending algorithm"
    - path: "backend/tests/unit/test_profile_service.py"
      provides: "ProfileService unit tests"
      contains: "Tests for profile CRUD, avatar updates"
    - path: "backend/tests/unit/test_follow_service.py"
      provides: "FollowService unit tests"
      contains: "Tests for follow/unfollow, rate limiting"
    - path: "backend/tests/unit/test_compilation_service.py"
      provides: "CompilationService unit tests"
      contains: "Tests for compilation workflow (with mocked container execution)"
    - path: "backend/tests/unit/api/"
      provides: "API endpoint tests"
      contains: "Tests for all routers using TestClient"
  key_links:
    - from: "test_factories.py"
      to: "all unit tests"
      via: "factory function imports"
      pattern: "from tests.test_factories import create_user, create_notebook"
    - from: "conftest.py"
      to: "all unit tests"
      via: "pytest fixtures"
      pattern: "db_session, mock_docker_client, mock_s3_client, mock_redis"
---

<objective>
Create comprehensive unit tests for all backend services and API endpoints

Purpose: Achieve 80%+ line coverage for backend code by testing all service layer business logic and API endpoints. Tests use aggressive mocking for external services per D-02 and factory functions for test data per D-03.

Output: 20+ test files covering all services and endpoints, with 80%+ line coverage and 70%+ branch coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-quality/05-CONTEXT.md
@.planning/phases/05-testing-quality/05-RESEARCH.md
@backend/pytest.ini
@backend/tests/conftest.py
@backend/tests/test_factories.py

# Locked Decisions from CONTEXT.md
- D-02: Aggressive mocking for external services (OAuth, S3, Meilisearch, Redis)
- D-03: Factory pattern for test data
- D-04: Database isolation via rollback transactions (already in conftest.py)

# Services to Test (from backend/app/services/)
- auth_service.py (5372 bytes)
- notebook_service.py (8640 bytes)
- fork_service.py (7503 bytes)
- like_service.py (3987 bytes)
- comment_service.py (8024 bytes)
- search_service.py (7576 bytes)
- feed_service.py (17097 bytes)
- trending_service.py (9672 bytes)
- profile_service.py (4283 bytes)
- follow_service.py (4764 bytes)
- compilation_service.py (10701 bytes)
- dataset_service.py (6078 bytes)
- storage_service.py (7077 bytes)
- cdn_service.py (6495 bytes)

# API Routers to Test (from backend/app/api/v1/)
- auth/router.py (OAuth endpoints)
- notebooks/router.py (CRUD operations)
- likes/router.py (Like/unlike)
- comments/router.py (Create, list, reply)
- profiles/router.py (View, edit profile)
- follows/router.py (Follow/unfollow)
- search/router.py (Search, filter)
- feed/router.py (Feed, trending)
- compilation/router.py (Compile, publish)
- datasets/router.py (Upload, list, delete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for AuthService</name>
  <files>backend/tests/unit/test_auth_service.py</files>
  <action>
    Create backend/tests/unit/test_auth_service.py with tests for:

    1. OAuth user creation (test_create_user_from_oauth):
       - Mock authlib OAuth client
       - Verify new user created with correct oauth_provider and oauth_id
       - Verify username generated from email if not provided
       - Test with both Google and Facebook providers

    2. Existing user login (test_get_user_by_oauth):
       - Create user via factory
       - Mock OAuth response returning existing oauth_id
       - Verify existing user returned

    3. Token validation (test_validate_token):
       - Mock python-jose decode
       - Test valid token returns user
       - Test expired token returns None
       - Test invalid token returns None

    4. Session creation (test_create_session):
       - Mock python-jose encode
       - Verify access token and refresh token created
       - Verify token expiration times (15 min access, 7 day refresh)

    Use factory functions: create_user() for test data
    Use mocks: patch('app.services.auth_service.jose') for JWT operations
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_auth_service.py -v --cov=app.services.auth_service --cov-report=term-missing</automated>
  </verify>
  <done>AuthService has unit tests covering OAuth flow, user creation, and token validation with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for NotebookService</name>
  <files>backend/tests/unit/test_notebook_service.py</files>
  <action>
    Create backend/tests/unit/test_notebook_service.py with tests for:

    1. Create notebook (test_create_notebook):
       - Use factory: create_user()
       - Verify notebook created with correct user_id
       - Verify title, description saved
       - Verify is_published defaults to False

    2. Get notebook by ID (test_get_notebook):
       - Use factory: create_notebook()
       - Verify notebook returned correctly
       - Test with non-existent ID returns None

    3. Update notebook (test_update_notebook):
       - Use factory: create_notebook()
       - Verify title and description updated
       - Verify only owner can update (403 for non-owner)

    4. Delete notebook (test_delete_notebook):
       - Use factory: create_notebook()
       - Verify notebook deleted from database
       - Test cascade delete: verify cells also deleted
       - Test delete protection: cannot delete if has forks (403 error)

    5. List notebooks (test_list_notebooks):
       - Create multiple notebooks via factory
       - Verify pagination works (limit, offset)
       - Verify is_published filter works
       - Verify user filter works

    Use factory functions: create_user, create_notebook, create_notebook_cell
    Use db_session fixture for rollback isolation
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_notebook_service.py -v --cov=app.services.notebook_service --cov-report=term-missing</automated>
  </verify>
  <done>NotebookService has unit tests covering all CRUD operations with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for ForkService</name>
  <files>backend/tests/unit/test_fork_service.py</files>
  <action>
    Create backend/tests/unit/test_fork_service.py with tests for:

    1. Create fork (test_create_fork):
       - Use factory: create_notebook() as parent
       - Use factory: create_user() as fork owner
       - Verify fork created with parent_id and root_id
       - Verify root_id = parent_id if parent is original notebook
       - Verify root_id preserved if parent is already a fork

    2. Fork copies cells (test_fork_copies_cells):
       - Create notebook with multiple cells via factory
       - Create fork
       - Verify all cells copied to fork
       - Verify cell order preserved (order_index)

    3. Delete protection (test_delete_protected_notebook_with_forks):
       - Create notebook via factory
       - Create fork of notebook
       - Attempt to delete original
       - Verify 403 error (cannot delete notebooks with forks)
       - Verify fork can still be accessed

    4. Fork chain traversal (test_get_fork_chain):
       - Create original notebook
       - Create fork A from original
       - Create fork B from fork A
       - Verify get_fork_chain returns [original, A, B]

    Use factory functions: create_user, create_notebook, create_fork
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_fork_service.py -v --cov=app.services.fork_service --cov-report=term-missing</automated>
  </verify>
  <done>ForkService has unit tests covering forking logic, lineage tracking, and delete protection with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for LikeService and CommentService</name>
  <files>backend/tests/unit/test_like_service.py, backend/tests/unit/test_comment_service.py</files>
  <action>
    Create backend/tests/unit/test_like_service.py with tests for:

    1. Toggle like (test_toggle_like):
       - Use factory: create_notebook(), create_user()
       - Test like creation (first toggle)
       - Test like removal (second toggle)
       - Verify like_count updated correctly

    2. Check if liked (test_is_liked):
       - Create like via factory
       - Verify returns True for existing like
       - Verify returns False for non-existing like

    3. Unique constraint (test_duplicate_like):
       - Create like via factory
       - Attempt to create duplicate like
       - Verify integrity error raised

    Create backend/tests/unit/test_comment_service.py with tests for:

    1. Create comment (test_create_comment):
       - Use factory: create_notebook(), create_user()
       - Verify comment created with content
       - Verify parent_id None for top-level comment

    2. Create reply (test_create_reply):
       - Create parent comment via factory
       - Create reply with parent_id
       - Verify reply linked to parent
       - Verify depth limit enforced (max 3 levels)

    3. Get comment thread (test_get_comment_thread):
       - Create nested comments via factory (parent → reply → reply to reply)
       - Verify thread returns hierarchical structure
       - Verify depth limit enforced

    4. Delete comment (test_delete_comment):
       - Create comment via factory
       - Verify comment soft deleted (is_deleted=True)
       - Verify replies preserved when parent deleted

    Use factory functions: create_user, create_notebook, create_like, create_comment
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_like_service.py tests/unit/test_comment_service.py -v --cov=app.services.like_service --cov=app.services.comment_service --cov-report=term-missing</automated>
  </verify>
  <done>LikeService and CommentService have unit tests covering all social interactions with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 5: Create unit tests for SearchService and TrendingService</name>
  <files>backend/tests/unit/test_search_service.py, backend/tests/unit/test_trending_service.py</files>
  <action>
    Create backend/tests/unit/test_search_service.py with tests for:

    1. Index notebook (test_index_notebook):
       - Use factory: create_notebook()
       - Mock Meilisearch client
       - Verify document indexed with correct fields
       - Verify tags included in index

    2. Search notebooks (test_search_notebooks):
       - Mock Meilisearch search response
       - Test search by title
       - Test search by tags
       - Test search by author username
       - Test filter by fork_type (original/fork/all)

    3. Update index (test_update_notebook_index):
       - Mock Meilisearch update
       - Verify index updated on notebook edit

    4. Delete from index (test_delete_notebook_index):
       - Mock Meilisearch delete
       - Verify document removed from index

    Create backend/tests/unit/test_trending_service.py with tests for:

    1. Calculate engagement score (test_calculate_engagement):
       - Verify formula: engagement = (likes * 2) + (comments * 3)
       - Test with various like/comment counts

    2. Calculate time decay (test_calculate_decay):
       - Verify decay formula: engagement / pow((age_hours + 2), 1.5)
       - Test with fresh notebook (age = 0)
       - Test with old notebook (age = 24 hours)
       - Verify decay decreases over time

    3. Update trending cache (test_update_trending_cache):
       - Mock Redis client
       - Verify ZSET operations for ranking
       - Verify HASH operations for score storage
       - Verify TTL set to 24 hours

    4. Get trending notebooks (test_get_trending):
       - Mock Redis ZRANGE
       - Verify returns notebooks sorted by score
       - Test pagination (limit, offset)

    Use factory functions: create_user, create_notebook, create_like, create_comment
    Use mocks: patch('app.services.search_service.meilisearch.Client')
    Use mocks: patch('app.services.trending_service.redis_client')
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_search_service.py tests/unit/test_trending_service.py -v --cov=app.services.search_service --cov=app.services.trending_service --cov-report=term-missing</automated>
  </verify>
  <done>SearchService and TrendingService have unit tests covering search indexing and trending algorithm with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 6: Create unit tests for FeedService and FollowService</name>
  <files>backend/tests/unit/test_feed_service.py, backend/tests/unit/test_follow_service.py</files>
  <action>
    Create backend/tests/unit/test_feed_service.py with tests for:

    1. Generate personalized feed (test_generate_personalized_feed):
       - Use factory: create_user() as follower
       - Create multiple users and notebooks
       - Create follows to followed users
       - Verify feed includes followed content
       - Verify feed excludes non-followed content
       - Verify trending fallback when no follows

    2. Increment engagement (test_increment_engagement):
       - Mock Redis HINCRBY
       - Verify like increments engagement by 2
       - Verify comment increments engagement by 3
       - Verify view increments engagement by 1

    3. Get feed with cursor pagination (test_get_feed_cursor):
       - Create notebooks via factory
       - Verify cursor-based pagination works
       - Verify hasMore flag correct

    Create backend/tests/unit/test_follow_service.py with tests for:

    1. Follow user (test_follow_user):
       - Use factory: create_user() for follower and following
       - Verify follow created
       - Verify cannot follow self (400 error)
       - Verify cannot follow twice (unique constraint)

    2. Unfollow user (test_unfollow_user):
       - Create follow via factory
       - Verify follow deleted
       - Verify unfollow non-existing follow returns None

    3. Get followers/following (test_get_followers_following):
       - Create multiple follows via factory
       - Verify get_followers returns users following target
       - Verify get_following returns users target follows
       - Verify count-only mode returns counts only (per D-10)

    4. Rate limiting (test_follow_rate_limit):
       - Create 100 follows for user
       - Attempt 101st follow
       - Verify 429 error (rate limit exceeded)

    Use factory functions: create_user, create_notebook, create_follow
    Use mocks: patch('app.services.feed_service.redis_client')
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_feed_service.py tests/unit/test_follow_service.py -v --cov=app.services.feed_service --cov=app.services.follow_service --cov-report=term-missing</automated>
  </verify>
  <done>FeedService and FollowService have unit tests covering feed generation and follow logic with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 7: Create unit tests for ProfileService</name>
  <files>backend/tests/unit/test_profile_service.py</files>
  <action>
    Create backend/tests/unit/test_profile_service.py with tests for:

    1. Get profile (test_get_profile):
       - Use factory: create_user()
       - Verify profile returned with username, avatar, bio
       - Verify published_notebook_count included
       - Verify total_likes_received included

    2. Update profile (test_update_profile):
       - Use factory: create_user()
       - Test username update (unique constraint)
       - Test bio update
       - Test avatar_url update
       - Verify only owner can update (403 for non-owner)

    3. Profile completion (test_check_profile_completion):
       - Create user with missing bio
       - Verify is_complete = False
       - Update bio
       - Verify is_complete = True

    Use factory functions: create_user
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_profile_service.py -v --cov=app.services.profile_service --cov-report=term-missing</automated>
  </verify>
  <done>ProfileService has unit tests covering profile CRUD operations with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 8: Create unit tests for CompilationService and DatasetService</name>
  <files>backend/tests/unit/test_compilation_service.py, backend/tests/unit/test_dataset_service.py</files>
  <action>
    Create backend/tests/unit/test_compilation_service.py with tests for:

    1. Compile notebook (test_compile_notebook):
       - Use factory: create_notebook() with cells
       - Mock ContainerExecutor (already exists in conftest)
       - Mock StorageService for output upload
       - Verify compilation task created
       - Verify status set to 'compiling'
       - Verify output saved to S3 on success

    2. Handle compilation failure (test_compile_notebook_failure):
       - Mock ContainerExecutor to return non-zero exit code
       - Verify status set to 'failed'
       - Verify error message saved

    3. Publish notebook (test_publish_notebook):
       - Use factory: create_notebook() with successful compilation
       - Verify is_published set to True
       - Verify published_at set
       - Verify CDN cache invalidated

    Create backend/tests/unit/test_dataset_service.py with tests for:

    1. Upload dataset (test_upload_dataset):
       - Use factory: create_user()
       - Mock S3 upload_file
       - Verify dataset created with s3_key
       - Verify presigned URL generated
       - Verify row_count calculated (CSV parsing)

    2. Get dataset (test_get_dataset):
       - Create dataset via factory
       - Verify dataset returned
       - Verify fresh presigned URL generated

    3. List datasets (test_list_datasets):
       - Create multiple datasets via factory
       - Verify only user's datasets returned
       - Verify presigned URLs NOT included (security pattern)

    4. Delete dataset (test_delete_dataset):
       - Create dataset via factory
       - Verify dataset deleted from database
       - Verify file deleted from S3
       - Verify only owner can delete

    Use factory functions: create_user, create_notebook
    Use mocks: mock_docker_client, mock_s3_client (from conftest)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/test_compilation_service.py tests/unit/test_dataset_service.py -v --cov=app.services.compilation_service --cov=app.services.dataset_service --cov-report=term-missing</automated>
  </verify>
  <done>CompilationService and DatasetService have unit tests covering compilation workflow and dataset management with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 9: Create API endpoint tests for all routers</name>
  <files>backend/tests/unit/api/test_auth_router.py, backend/tests/unit/api/test_notebooks_router.py, backend/tests/unit/api/test_social_router.py, backend/tests/unit/api/test_feed_router.py</files>
  <action>
    Create backend/tests/unit/api/ directory with endpoint tests using FastAPI TestClient:

    test_auth_router.py:
    - POST /api/v1/auth/google (mock OAuth callback)
    - POST /api/v1/auth/facebook (mock OAuth callback)
    - GET /api/v1/auth/me (authenticated)
    - GET /api/v1/auth/me (unauthenticated returns 401)
    - POST /api/v1/auth/logout

    test_notebooks_router.py:
    - POST /api/v1/notebooks (create notebook)
    - GET /api/v1/notebooks (list notebooks with pagination)
    - GET /api/v1/notebooks/{id} (get notebook)
    - PUT /api/v1/notebooks/{id} (update notebook)
    - DELETE /api/v1/notebooks/{id} (delete notebook)
    - POST /api/v1/notebooks/{id}/cells (add cell)
    - PUT /api/v1/notebooks/{id}/cells/{cell_id} (update cell)
    - DELETE /api/v1/notebooks/{id}/cells/{cell_id} (delete cell)

    test_social_router.py (likes and comments):
    - POST /api/v1/likes (toggle like)
    - GET /api/v1/comments (list comments for notebook)
    - POST /api/v1/comments (create comment)
    - POST /api/v1/comments/{id}/replies (create reply)

    test_feed_router.py:
    - GET /api/v1/feed (personalized feed)
    - GET /api/v1/feed/trending (trending notebooks)
    - POST /api/v1/engagement (track view)

    test_search_router.py:
    - GET /api/v1/search (search notebooks)
    - Test query parameters: q, tags, fork_type

    test_profiles_router.py:
    - GET /api/v1/profiles/{id} (public profile view)
    - PUT /api/v1/profiles/me (update own profile)

    test_follows_router.py:
    - POST /api/v1/follows (follow user)
    - DELETE /api/v1/follows/{id} (unfollow)
    - GET /api/v1/followers/{id} (get followers count)
    - GET /api/v1/following/{id} (get following count)

    Use TestClient from fastapi.testclient
    Use auth_headers fixture for authenticated requests
    Test both success and error cases (400, 401, 403, 404, 429)
  </action>
  <verify>
    <automated>cd /Volumes/SSDX/codes/time/backend && pytest tests/unit/api/ -v --cov=app.api --cov-report=term-missing</automated>
  </verify>
  <done>All API routers have endpoint tests covering success and error cases with 80%+ coverage</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. All service tests pass:
   - pytest tests/unit/test_*_service.py -v shows all tests passing
   - Coverage report shows 80%+ line coverage for all services

2. All API endpoint tests pass:
   - pytest tests/unit/api/ -v shows all tests passing
   - Coverage report shows 80%+ line coverage for all routers

3. Total test count:
   - pytest tests/unit/ --collect-only | grep "test session starts" -A 5 shows 100+ tests

4. Coverage thresholds met:
   - pytest --cov=app --cov-report=term shows 80%+ line coverage
   - Branch coverage 70%+

5. All external services mocked:
   - No real OAuth calls, S3 uploads, Meilisearch queries, Redis operations
   - All mocks defined in fixtures or test methods
</verification>

<success_criteria>
1. 20+ test files created covering all services and API endpoints
2. 100+ unit tests written and passing
3. 80%+ line coverage achieved for backend code
4. 70%+ branch coverage achieved for backend code
5. All external services mocked (OAuth, S3, Meilisearch, Redis)
6. Factory functions used for all test data generation
7. Database isolation via rollback transactions verified
8. CI/CD workflow (test-backend.yml) passes when run
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-quality/05-01-SUMMARY.md` with:
- Summary of backend unit tests created
- Coverage report by service
- List of API endpoints tested
- Any uncovered code paths and rationale
- Total test count
</output>
